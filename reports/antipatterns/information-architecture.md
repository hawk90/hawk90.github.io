# 정보구조 안티패턴 — AP-I-01..20 처분 보고

- 대상: `information_architecture` 레지스트리 160건 중 **20건**
- 검증일: 2026-08-20
- 배포: `0dda5355` (GitHub Pages, 성공)
- 레지스트리: `archives/.../category-registries/information_architecture.json`

측정 기준은 **빌드된 사이트**입니다. 안티패턴 문서가 결함이라고 서술한 것이 아니라, `dist/`에서 재현 가능하게 잰 것만 결함으로 취급했습니다. 각 항목의 근거·검증 명령·잔여 위험은 레지스트리 JSON에 항목별로 들어 있습니다.

---

## 1. 발견한 항목

측정으로 확인된 결함입니다. 괄호 안은 발견 당시 수치입니다.

| 항목 | 무엇이 문제였나 |
|---|---|
| I-03 | 폴더·URL·분류가 한 문자열. 파일을 옮기지 않으면 재분류 불가, 옮기면 URL 파손 |
| I-04 | `topics`가 3387편에 **필수**이고 레지스트리 검증까지 받는데 읽는 페이지가 0개 |
| I-01 · I-05 | 65개 카테고리 중 **54개**가 글 0편인데 페이지 생성 + sitemap 등재 + 전 페이지 사이드바 링크. 개수 배지는 0일 때 숨겨져 클릭 전엔 안 보임 |
| I-07 | 한 개념이 두 태그 URL로 분열 **10건**. 최악은 `/tags/cpp`(219편) 대 `/tags/c++`(9편) |
| I-15 | `/tags/make`와 `/tags/makefile`이 **같은 7편**을 렌더. 철자를 공유하지 않아 기존 탐지기가 못 봄 |
| I-19 | `/recently-updated`가 빌드·색인 요청되면서 **인바운드 링크 0** |
| (파생) | 포스트 URL 안의 중간 구간 **28개가 404**. 합치면 발행 726편 전부를 덮음 |
| (파생) | `/components`(테마 참조)가 색인 요청되면서 링크 0 |

## 2. 실제로 수정한 항목

**11건 `remediated`** — 커밋 3개.

| 커밋 | 내용 |
|---|---|
| `21d6eafc` | 카테고리 소속을 `topics`에서 읽도록 전환. 빈 카테고리 페이지 생성 중단. `/recently-updated` 링크 |
| `3232b1be` | 태그 분열 병합 — 정본 딕셔너리 + preview-first 스크립트 + 회귀 감지 |
| `0dda5355` | 죽은 URL 구간 28개 해소. sitemap↔robots 일치 강제. `/components` noindex |

전환 전 **726편 전부에서 경로 기반과 `topics` 기반 버킷이 0건 불일치**임을 확인했습니다. 화면은 그대로고, 앞으로 둘이 달라질 수 있다는 것만 바뀝니다.

태그 병합은 **드래프트까지** 적용했습니다. `C++`가 드래프트 281편에 있어, 발행분만 고치면 그것들이 올라올 때 분열이 돌아옵니다.

## 3. 수정하지 않은 항목과 이유

**8건 `accepted`** — 이 저장소에는 해당하지 않거나, 독자에게 도달하지 않는 것입니다.

| 항목 | 왜 결함이 아닌가 | 재검토 조건 |
|---|---|---|
| I-18 무한 스크롤 | 코드에 존재하지 않음. 모든 목록이 URL을 가진 정적 라우트 | 클라이언트 append 도입 시 |
| I-20 인기순 편향 | 애널리틱스가 꺼져 있어 인기 데이터 자체가 없음 | 조회수 지표가 정렬에 들어올 때 |
| I-06 태그 롱테일 | 1편짜리 키 997개는 **페이지도 링크도 생기지 않음** | 2편짜리 페이지가 60% 초과 시 |
| I-08 버전 태그 | 467개 페이지 중 3개(0.64%), C++ 표준이라 진짜 구분 | 한 제품이 버전 태그 3개 이상 누적 시 |
| I-10 태그 클라우드 | 인덱스 페이지 없음, 빈도 크기 조절 없음, 칩 12개가 카테고리 트리 아래 | `/tags` 인덱스 추가 시 |
| I-02 이름 모호성 | 겹치는 쌍마다 한쪽만 발행 글 보유 | 양쪽 다 발행 글을 가질 때 |
| I-15 목록 중복 | 4개 카테고리가 시리즈 페이지와 동일 집합이나, 시리즈 쪽이 더 풍부해 부가 잡음 | 다섯 번째가 생길 때 |
| I-17 페이지네이션 | 데스크톱에서 경로가 5개(카테고리 트리·시리즈·검색·경로·아카이브) | — |

I-17에는 **실제 잔여 결함**이 있습니다. 대안 링크 106개가 전부 `hidden lg:block` 안에 있어, lg 미만에서는 목록 페이지가 정확히 안티패턴대로 퇴화합니다. 페이지네이션 문제가 아니라 **모바일 내비게이션 과제**로 분리했습니다.

## 4. 추가 결정이 필요한 항목

**1건 `routed`.**

**I-09** — `cpp` 태그 219편이 5개 시리즈·3개 최상위 카테고리에 걸쳐 있는데, 유일한 교차 진입점이 설명 없는 페이지네이션 태그 목록입니다. 고치려면 학습경로를 손대야 하고, 그건 발행 정책 결정에 걸려 있습니다.

**학습경로 자체 — 보류.** 경로 4개가 참조하는 45개 시리즈 중 발행된 건 8개뿐이고, 동시성 모델 경로는 살아 있는 시리즈 링크가 **0개**입니다. 경로는 드래프트 코퍼스를 향해 설계돼 있습니다. 선택지는 (a) 발행된 글 기준으로 경로 재작성, (b) 드래프트 발행, (c) 현상 유지. **(b)는 사업 판단이라 제 범위 밖입니다.**

**표기 흔들림 30건 — 분리.** `Debugging`/`debugging`, `C`/`c` 등. 이미 **같은 페이지**로 라우팅되고 배지 라벨만 빌드 순서에 좌우됩니다. 승자가 갈려서(`C`=82:19 대문자 우세, `debugging`=56:17 소문자 우세) 일괄 규칙이 없습니다. 라우팅 무관이므로 별도 콘텐츠 정비 작업입니다.

**후속 1건.** `audit:series`(챕터 순서)가 lefthook·CI·`verify:release` 어디에도 없습니다. 지금은 28개 시리즈 전부 1..N 연속이지만, 깨지면 `dist`까지 도달합니다. `verify-release.mjs`에 한 줄 추가하면 됩니다.

## 5. 검증 결과

배포 후 `dist/` 기준 (1451 페이지).

| 검사 | 결과 |
|---|---|
| 발행 포스트 전부 빌드 | **726 / 726**, 누락 0 |
| 죽은 URL 구간 | 28 → **0** |
| 빈 카테고리 페이지 | 54 → **0** |
| 사이드바 카테고리 링크 | 65 → **11**, 빈 곳 가리키는 것 0 |
| 태그 개념 분열 | 10 → **0** |
| 태그 URL 키 / 페이지 | 1465 → 1456 / 468 → 467 |
| sitemap 죽은 URL | **0** (1352건 전부 해석됨) |
| noindex인데 색인 요청 | **0** |
| 링크 없는 색인 페이지 | **0** |
| 미해결 내부 링크 | **0** |
| `verify:release` | 21단계 통과 |

`/recently-updated`는 인바운드 0 → **1421**(링크 추가), `/components`는 noindex(작업 도구라 색인에서 제외). 같은 결함, 의도에 따라 반대 처방입니다.

### 판단이 틀렸던 것

기록해 둡니다. 드라이런이 **가짜 병합 2건**을 잡았습니다.

- `typeid`(folly의 C++ 연산자) vs `type-id`(No-RTTI 설계의 수동 식별자) — 다른 개념
- `async-io`(folly::AsyncIO) vs `asyncio`(Python 모듈) — 다른 개념

`riscv` 병합은 **방향이 반대**였습니다. 66개 파일을 5개에 맞추면서 ISA 공식 이름을 버리는 것이었고, 근거로 든 카테고리 id는 다른 네임스페이스였습니다. 둘 다 `data/tag-aliases.yaml`에 이유와 함께 남겼습니다.

동일 목록 탐지기도 처음엔 25건 중 24건이 오탐이었습니다(`/tags/asan`·`/tags/ubsan`은 같은 2편을 덮지만 다른 것). **한 키가 다른 키로 시작할 것**을 추가해 좁혔고, 7개 파일에 `Makefile`을 되돌려 넣어 정확히 1건만 잡는 것을 확인했습니다.

---

## 남은 범위

| 카테고리 | 전체 | 완료 | 남음 | 우선순위 |
|---|---:|---:|---:|---|
| information_architecture | 160 | 20 | 140 | P1 |
| performance | 80 | 0 | 80 | P1 |
| content | 239 | 0 | 239 | P2 |
| ux | 103 | 0 | 103 | P2 |
| localization | 100 | 0 | 100 | P2 |
| observability | 100 | 0 | 100 | P2 |
| search_seo | 75 | 0 | 75 | P2 |
| metadata · quality · repository · security | 390 | 390 | 0 | — |
| **합계** | **1247** | **410** | **837** | |

837건을 소화하는 것이 목표가 아닙니다. 남은 항목은 **측정하지 않았으므로 처분하지 않았습니다** — 재지 않은 것에 상태를 부여하면 레지스트리가 확인되지 않은 사실을 주장하게 됩니다.

권장 순서는 위 표의 우선순위이되, 각 배치는 **빌드된 결과에서 잴 수 있는 것부터** 잡습니다. 이번 20건에서 반복해 확인된 것은, 감사가 존재하는데도 결함에 **닿지 못한** 경우가 많았다는 점입니다 — 마크다운 링크 감사는 템플릿이 만든 href를 못 보고, 렌더 감사는 링크 *이름*만 보고 도달 가능성은 안 봤으며, 태그 임계값은 라우트를 만드는 곳에만 적용되고 가리키는 곳에는 적용되지 않았습니다.
