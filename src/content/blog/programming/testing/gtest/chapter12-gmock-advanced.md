---
title: "Ch 12: gMock advanced"
date: 2026-05-21T12:00:00
description: "sequences, ON_CALL, mock actions, NiceMock/StrictMock 패턴."
series: "gtest 심화"
seriesOrder: 12
tags: [gtest, gmock, sequence, on-call, actions]
draft: true
---

> Outline — `InSequence`로 호출 순서 강제. `Sequence` 객체로 *복수 시퀀스 그래프*. `ON_CALL`(기본 동작) vs `EXPECT_CALL`(검증) 분리. mock actions — `Return`/`Invoke`/`DoAll`/`SaveArg`/`SetArgPointee`. `unique_ptr` 반환의 `Return(ByMove(...))`.
