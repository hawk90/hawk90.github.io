---
title: "Ch 14: Change"
date: 2026-05-13T14:00:00
description: "reduce를 Expression의 polymorphic 메서드로. Money.reduce / Sum.reduce."
series: "TDD by Example"
seriesOrder: 14
tags: [tdd, testing, kent-beck, polymorphism]
type: book-review
bookTitle: "Test-Driven Development: By Example"
bookAuthor: "Kent Beck"
---

> **Test-Driven Development by Example** Chapter 14 요약

## 이 챕터의 메시지

`Bank.reduce`가 `Sum`만 처리. Money 단독은? — Polymorphism.

```java
interface Expression {
    Money reduce(String to);
}

class Money implements Expression {
    public Money reduce(String to) {
        return this;   // 그대로
    }
}

class Sum implements Expression {
    public Money reduce(String to) {
        int amount = augend.amount + addend.amount;
        return new Money(amount, to);
    }
}
```

이제 Bank는:

```java
class Bank {
    Money reduce(Expression source, String to) {
        return source.reduce(to);
    }
}
```

Bank는 — Expression이 무엇인지 모름. 다형성에 위임.

## 정리

- reduce를 Expression의 메서드로
- Money / Sum 각자 자기 reduce
- Bank는 단순한 위임

## 다음 장 예고

다음 장은 **Mixed Currencies** — 다른 통화 덧셈.

## 관련 항목

- [Ch 13: Make It](/blog/programming/engineering/tdd-by-example/ch13)
- [GoF Visitor](/blog/programming/design/gof-design-patterns/)
