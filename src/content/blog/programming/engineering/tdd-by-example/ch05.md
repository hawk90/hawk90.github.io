---
title: "Ch 5: Franc-ly Speaking"
date: 2026-05-13T05:00:00
description: "Franc 클래스 추가 — Dollar와 거의 같음. 중복이 명확해짐, 해결은 다음 장."
series: "TDD by Example"
seriesOrder: 5
tags: [tdd, testing, kent-beck, duplication]
type: book-review
bookTitle: "Test-Driven Development: By Example"
bookAuthor: "Kent Beck"
draft: true
---

> **Test-Driven Development by Example** Chapter 5 요약

## 이 챕터의 메시지

원래 요구사항 — **다중 통화**. Dollar만으로는 부족.

## Franc 추가

```java
public void testFrancMultiplication() {
    Franc five = new Franc(5);
    assertTrue(new Franc(10).equals(five.times(2)));
}
```

`Franc` 클래스 — Dollar와 거의 같음.

```java
class Franc {
    private int amount;
    Franc(int amount) { this.amount = amount; }
    Franc times(int multiplier) {
        return new Franc(amount * multiplier);
    }
    public boolean equals(Object obj) {
        Franc franc = (Franc) obj;
        return amount == franc.amount;
    }
}
```

**거의 Dollar 복사**. 중복이 명확.

## 일단 그대로

TDD의 원칙 — **한 번에 한 가지**.

```
지금: Franc 테스트 통과
나중: 중복 해결
```

당장은 — 중복을 받아들임. 그러나 To-Do에 추가.

```
[X] Franc × 2 = $10
[ ] Dollar / Franc 중복 제거    ← 새로
```

## 중복의 신호

같은 코드가 두 곳에 → **공통 추상**이 숨어 있다.

다음 장에서 — `Money` 추상으로 끌어 올림.

## 정리

- Franc 추가 — Dollar와 같은 패턴
- **중복 명확**해짐
- 일단 그대로, 다음 단계에 해결

## 다음 장 예고

다음 장은 **Equality for All, Redux** — 중복 해결 시작.

## 관련 항목

- [Ch 4: Privacy](/blog/programming/engineering/tdd-by-example/ch04)
- [Refactoring Ch 3: Code Smells](/blog/programming/design/refactoring/ch03) — Duplicated Code
