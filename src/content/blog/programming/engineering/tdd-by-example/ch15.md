---
title: "Ch 15: Mixed Currencies"
date: 2026-05-10T15:00:00
description: "$5 + 10 CHF = $10 (환율 2:1). Bank.addRate / reduce 통합."
series: "TDD by Example"
seriesOrder: 15
tags: [tdd, testing, kent-beck, exchange-rate]
type: book-review
bookTitle: "Test-Driven Development: By Example"
bookAuthor: "Kent Beck"
draft: true
---

> **Test-Driven Development by Example** Chapter 15 요약

## 이 챕터의 메시지

원래 목표 — 다중 통화 덧셈.

```java
public void testMixedAddition() {
    Expression fiveBucks = Money.dollar(5);
    Expression tenFrancs = Money.franc(10);
    Bank bank = new Bank();
    bank.addRate("CHF", "USD", 2);
    Money result = bank.reduce(fiveBucks.plus(tenFrancs), "USD");
    assertEquals(Money.dollar(10), result);
}
```

`addRate(from, to, rate)` — 환율 등록. `reduce`가 환율 적용.

## Bank의 환율

```java
class Bank {
    Map<Pair, Integer> rates = new HashMap<>();
    
    void addRate(String from, String to, int rate) {
        rates.put(new Pair(from, to), rate);
    }
    
    int rate(String from, String to) {
        if (from.equals(to)) return 1;
        return rates.get(new Pair(from, to));
    }
}
```

## Money.reduce

```java
class Money implements Expression {
    public Money reduce(Bank bank, String to) {
        int rate = bank.rate(currency, to);
        return new Money(amount / rate, to);
    }
}
```

Sum.reduce도 — augend / addend 각각 reduce 후 합산.

## 메인 목표 달성

원래 책 시작 목표 — $5 + 10 CHF = $10 (환율 2:1) — 이제 동작.

## 정리

- Bank에 환율 저장 (`addRate`)
- reduce 시 — 환율 적용
- 다중 통화 덧셈 완성

## 다음 장 예고

다음 장은 **Abstraction, Finally** — Times 메서드 일반화.

## 관련 항목

- [Ch 14: Change](/blog/programming/engineering/tdd-by-example/ch14)
