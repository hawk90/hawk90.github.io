---
title: "Ch 8: Makin' Objects"
date: 2026-05-13T08:00:00
description: "Factory method — Money.dollar(), Money.franc(). 서브클래스가 숨겨짐."
series: "TDD by Example"
seriesOrder: 8
tags: [tdd, testing, kent-beck, factory]
type: book-review
bookTitle: "Test-Driven Development: By Example"
bookAuthor: "Kent Beck"
draft: true
---

> **Test-Driven Development by Example** Chapter 8 요약

## 이 챕터의 메시지

Dollar / Franc 클래스를 — 사용자가 알 필요 없게.

> **Factory Method**로 객체 생성 추상화.

## Factory 메서드 추가

```java
class Money {
    static Money dollar(int amount) {
        return new Dollar(amount, "USD");
    }
    
    static Money franc(int amount) {
        return new Franc(amount, "CHF");
    }
}
```

테스트:

```java
public void testMultiplication() {
    Money five = Money.dollar(5);
    assertEquals(Money.dollar(10), five.times(2));
}
```

이제 사용자는 — Dollar / Franc 클래스를 모름. Money 인터페이스만.

## 반환 타입 변경

`times`는 이제 — `Money` 반환.

```java
class Dollar extends Money {
    Money times(int multiplier) {
        return new Dollar(amount * multiplier, currency);
    }
}
```

서브타입 정보가 점차 사라짐.

## To-Do 진행

```
[X] Currency 도메인 개념
[ ] Dollar / Franc 클래스 자체를 합칠 수 있나?
[ ] 환율 적용
```

Dollar와 Franc의 — `times` 빼고는 거의 동일. 다음 장에서 합치기 시도.

## 정리

- **Factory Method** — `Money.dollar()`, `Money.franc()`
- 서브타입을 사용자에게 숨김
- 인터페이스 — Money 하나

## 다음 장 예고

다음 장은 **Times We're Livin' In** — Dollar와 Franc 합치기.

## 관련 항목

- [Ch 7: Apples and Oranges](/blog/programming/engineering/tdd-by-example/ch07)
- [GoF Factory Method](/blog/programming/design/gof-design-patterns/item01-abstract-factory/)
