---
title: "Ch 13: Make It"
date: 2026-05-10T13:00:00
description: "Expression 인터페이스 + Sum 클래스. Money와 Sum 모두 Expression."
series: "TDD by Example"
seriesOrder: 13
tags: [tdd, testing, kent-beck, expression, sum]
type: book-review
bookTitle: "Test-Driven Development: By Example"
bookAuthor: "Kent Beck"
draft: true
---

> **Test-Driven Development by Example** Chapter 13 요약

## 이 챕터의 메시지

```java
interface Expression {}

class Money implements Expression {
    Expression plus(Money addend) {
        return new Sum(this, addend);
    }
}

class Sum implements Expression {
    Money augend, addend;
    Sum(Money augend, Money addend) { ... }
}
```

이제 Money는 Expression의 한 종류, Sum도 Expression.

## reduce 메서드

```java
class Bank {
    Money reduce(Expression source, String to) {
        Sum sum = (Sum) source;
        int amount = sum.augend.amount + sum.addend.amount;
        return new Money(amount, to);
    }
}
```

테스트:

```java
Money five = Money.dollar(5);
Expression sum = five.plus(five);
Bank bank = new Bank();
Money result = bank.reduce(sum, "USD");
assertEquals(Money.dollar(10), result);
```

## Expression 패턴

이게 — **Interpreter** 또는 **Composite** 패턴의 응용. 식이 — 객체로 표현됨.

## 정리

- Expression 인터페이스
- Money + Sum 모두 Expression
- Bank.reduce로 — 실제 값 계산

## 다음 장 예고

다음 장은 **Change** — Reduce 일반화.

## 관련 항목

- [Ch 12: Addition](/blog/programming/engineering/tdd-by-example/ch12)
