---
title: "Chapter 11: Network Programming"
date: 2026-05-13
description: "네트워크 프로그래밍 - 소켓과 클라이언트-서버"
series: "Computer Systems: A Programmer's Perspective"
seriesOrder: 11
tags: [csapp, computer-systems, network, socket, tcp]
---

> **CSAPP** Chapter 11 요약

## 핵심 주장

**네트워크도 파일이다.** 소켓 인터페이스로 클라이언트-서버 통신을 구현한다.

## 클라이언트-서버 모델

### 기본 개념

```
클라이언트 ←→ 서버
  요청 →
  ← 응답
```

트랜잭션:
1. 클라이언트 요청
2. 서버 처리
3. 서버 응답
4. 클라이언트 수신

## 네트워크 기초

### 네트워크 계층

```
+-------------------+
| 응용 계층 (HTTP)   |
+-------------------+
| 전송 계층 (TCP)    |
+-------------------+
| 네트워크 계층 (IP) |
+-------------------+
| 링크 계층 (이더넷) |
+-------------------+
```

### IP 주소

32비트 주소:
- 점 표기법: 128.2.194.242
- 빅 엔디안 (네트워크 바이트 순서)

```c
struct in_addr {
    uint32_t s_addr;
};
```

### 도메인 이름

DNS 변환:
```
www.example.com → 93.184.216.34
```

```c
struct hostent *gethostbyname(const char *name);
```

### 포트

서비스 구분:
- HTTP: 80
- HTTPS: 443
- SSH: 22
- 에페머럴 포트: 49152-65535

## 소켓 인터페이스

### 소켓이란?

통신의 끝점:
- (IP 주소, 포트) 쌍
- 파일 디스크립터로 표현
- 읽기/쓰기 가능

### 소켓 주소 구조

```c
struct sockaddr_in {
    uint16_t sin_family;   // AF_INET
    uint16_t sin_port;     // 포트 번호
    struct in_addr sin_addr; // IP 주소
    char sin_zero[8];
};
```

## 클라이언트 구현

### 연결 과정

```c
// 1. 소켓 생성
int sockfd = socket(AF_INET, SOCK_STREAM, 0);

// 2. 서버 주소 설정
struct sockaddr_in serveraddr;
serveraddr.sin_family = AF_INET;
serveraddr.sin_port = htons(port);
inet_pton(AF_INET, "127.0.0.1", &serveraddr.sin_addr);

// 3. 연결
connect(sockfd, (struct sockaddr *)&serveraddr, sizeof(serveraddr));

// 4. 통신
write(sockfd, request, strlen(request));
read(sockfd, response, MAXBUF);

// 5. 종료
close(sockfd);
```

### 헬퍼 함수

```c
int open_clientfd(char *hostname, char *port) {
    struct addrinfo hints, *listp, *p;
    int clientfd;

    memset(&hints, 0, sizeof(hints));
    hints.ai_socktype = SOCK_STREAM;
    hints.ai_flags = AI_NUMERICSERV | AI_ADDRCONFIG;

    getaddrinfo(hostname, port, &hints, &listp);

    for (p = listp; p; p = p->ai_next) {
        if ((clientfd = socket(p->ai_family, p->ai_socktype,
                               p->ai_protocol)) < 0)
            continue;
        if (connect(clientfd, p->ai_addr, p->ai_addrlen) != -1)
            break;
        close(clientfd);
    }

    freeaddrinfo(listp);
    return clientfd;
}
```

## 서버 구현

### 서버 과정

```c
// 1. 소켓 생성
int listenfd = socket(AF_INET, SOCK_STREAM, 0);

// 2. 주소 바인딩
struct sockaddr_in serveraddr;
serveraddr.sin_family = AF_INET;
serveraddr.sin_port = htons(port);
serveraddr.sin_addr.s_addr = htonl(INADDR_ANY);
bind(listenfd, (struct sockaddr *)&serveraddr, sizeof(serveraddr));

// 3. 리스닝 시작
listen(listenfd, LISTENQ);

// 4. 연결 수락 루프
while (1) {
    struct sockaddr_in clientaddr;
    socklen_t clientlen = sizeof(clientaddr);
    int connfd = accept(listenfd, (struct sockaddr *)&clientaddr, &clientlen);

    // 5. 클라이언트 처리
    handle_client(connfd);

    close(connfd);
}
```

### 헬퍼 함수

```c
int open_listenfd(char *port) {
    struct addrinfo hints, *listp, *p;
    int listenfd, optval = 1;

    memset(&hints, 0, sizeof(hints));
    hints.ai_socktype = SOCK_STREAM;
    hints.ai_flags = AI_PASSIVE | AI_ADDRCONFIG | AI_NUMERICSERV;

    getaddrinfo(NULL, port, &hints, &listp);

    for (p = listp; p; p = p->ai_next) {
        if ((listenfd = socket(p->ai_family, p->ai_socktype,
                               p->ai_protocol)) < 0)
            continue;
        setsockopt(listenfd, SOL_SOCKET, SO_REUSEADDR,
                   &optval, sizeof(optval));
        if (bind(listenfd, p->ai_addr, p->ai_addrlen) == 0)
            break;
        close(listenfd);
    }

    freeaddrinfo(listp);
    listen(listenfd, LISTENQ);
    return listenfd;
}
```

## 에코 서버/클라이언트

### 에코 서버

```c
void echo(int connfd) {
    char buf[MAXLINE];
    ssize_t n;

    while ((n = read(connfd, buf, MAXLINE)) > 0) {
        printf("Received: %s", buf);
        write(connfd, buf, n);
    }
}
```

### 에코 클라이언트

```c
void client(int clientfd) {
    char buf[MAXLINE];

    while (fgets(buf, MAXLINE, stdin) != NULL) {
        write(clientfd, buf, strlen(buf));
        read(clientfd, buf, MAXLINE);
        printf("Echo: %s", buf);
    }
}
```

## 웹 기초

### HTTP

요청:
```
GET /index.html HTTP/1.1
Host: www.example.com
```

응답:
```
HTTP/1.1 200 OK
Content-Type: text/html
Content-Length: 1234

<html>...</html>
```

### 동적 컨텐츠

CGI (Common Gateway Interface):
- 서버가 프로그램 실행
- 환경 변수로 정보 전달
- stdout이 응답

## 시사점

1. **소켓 = 파일**: read/write로 통신
2. **클라이언트**: connect
3. **서버**: bind, listen, accept
4. **HTTP**: 텍스트 기반 프로토콜

## 인용

> "A socket is an end point of a connection."

> "All network applications are based on the client-server model."

## 연결

- [Chapter 10: System-Level I/O](./ch10) — 파일 I/O
- [Chapter 12: Concurrent Programming](./ch12) — 동시성 서버

