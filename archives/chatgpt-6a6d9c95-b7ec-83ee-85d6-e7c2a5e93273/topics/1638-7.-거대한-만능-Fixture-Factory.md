---
title: "7. 거대한 만능 Fixture Factory"
source_message: 73
source_role: assistant
---

# 7. 거대한 만능 Fixture Factory

다음 함수가 계속 커질 수 있다.

```ts
createDocumentFixture({
  id,
  slug,
  title,
  topic,
  type,
  status,
  draft,
  dates,
  relations,
  headings,
  links,
  environment,
  ...
})
```

모든 테스트가 같은 Factory의 기본값에 의존하면 Domain 모델 변경 시 수백 개 테스트가 함께 깨진다.

또 기본값이 테스트 의미를 숨긴다.

```ts
createDocumentFixture({
  status: "historical",
});
```

이 문서가 어떤 Topic과 날짜를 가졌는지 알기 어렵다.

### 권장

작은 기본 Builder를 두되 중요한 조건은 테스트에서 명시한다.

```ts
const document =
  aCurrentConcept()
    .withId("pcie-bar")
    .withTopic("pcie-cxl")
    .build();
```

Builder 자체를 복잡한 DSL로 만들 필요는 없다.

---
