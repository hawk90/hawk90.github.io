---
title: "65. Featured 테스트"
source_message: 62
source_role: assistant
---

# 65. Featured 테스트

```ts
it("rejects a historical document in home featured curation", () => {
  const document =
    createDocumentFixture({
      id: "old-guide",
      status: "historical",
    });

  const manifest =
    createContentManifest([
      document,
    ]);

  expect(() =>
    getFeaturedDocuments(
      ["old-guide"],
      manifest,
      testContext,
    )
  ).toThrow(
    /cannot be featured/,
  );
});
```

---
