---
title: "Task 3-24. 배포 안정성·장애 격리·복구 가능성 안티패턴"
source_message: 71
source_role: assistant
---

# Task 3-24. 배포 안정성·장애 격리·복구 가능성 안티패턴

이번에는 콘텐츠 구조, 검색, 마이그레이션, 의존성 정리와 겹치지 않는 **운영 안정성**을 본다.

정적 사이트는 서버가 없으니 장애가 적어 보이지만, 실제로는 다음 요소가 동시에 맞아야 한다.

```text
Markdown
→ Astro Build
→ 생성 자산
→ GitHub Actions
→ GitHub Pages 배포
→ CDN Cache
→ Custom Domain·HTTPS
→ 외부 댓글·광고·분석
```

이 중 하나만 잘못돼도 전체 사이트가 깨지거나, 이전 버전으로 되돌아가기 어려워질 수 있다.

핵심 목표는 다음이다.

> 빌드가 성공했다는 사실보다, 검증한 산출물을 안전하게 배포하고 문제가 생기면 빠르게 이전 정상 버전으로 복구할 수 있어야 한다.

---

## 1. Build 성공을 배포 성공으로 착각

다음은 서로 다른 상태다.

```text
TypeScript 통과
Astro Build 성공
dist 검증 성공
GitHub Pages 업로드 성공
실제 운영 URL 정상
```

`astro build`가 성공했다고 다음이 보장되지는 않는다.

```text
잘못된 base path
404 자산
빈 검색 Index
잘못된 canonical
누락된 CSS
잘못된 GitHub Pages 경로
```

따라서 배포 파이프라인은 최소한 세 단계로 나눈다.

```text
Build
→ Dist Validation
→ Deploy
```

---
