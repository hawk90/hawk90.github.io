---
title: "95. Redirect Registry 연결"
source_message: 62
source_role: assistant
---

# 95. Redirect Registry 연결

```ts
export interface RedirectEntry {
  from: string;
  to: string;
  reason:
    | "renamed"
    | "merged"
    | "superseded"
    | "moved";
}
```

Manifest validation에서:

```text
redirect target가 존재하는가
redirect source가 현재 canonical URL과 충돌하는가
cycle이 있는가
```

를 검사한다.

---
