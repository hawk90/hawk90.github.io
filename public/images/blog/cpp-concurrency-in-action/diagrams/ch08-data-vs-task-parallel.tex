\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  dcell/.style={rectangle, draw=clibord, fill=clifill, line width=0.4pt,
                minimum width=7mm, minimum height=7mm, inner sep=0pt,
                font=\sffamily\footnotesize, rounded corners=1pt},
  thread/.style={rectangle, rounded corners=2pt, draw=impbord, fill=impfill,
                 line width=0.4pt, minimum width=22mm, minimum height=7mm,
                 inner sep=2pt, font=\sffamily\footnotesize, align=center},
  op/.style={rectangle, rounded corners=2pt, draw=hibord, fill=hifill,
             line width=0.4pt, minimum width=22mm, minimum height=7mm,
             inner sep=2pt, font=\sffamily\footnotesize, align=center},
  fan/.style={-{Stealth[length=1.5mm]}, draw=edgesc, line width=0.3pt}
]

% =========== LEFT: Data Parallel ===========
\node[font=\sffamily\bfseries\small] at (-3.5, 4.5) {Data Parallel};

% Data row (8 items)
\foreach \i in {1,2,...,8} {
  \node[dcell] (d\i) at (-7 + \i*0.8, 3.4) {\i};
}

% 4 threads + ops — each thread handles 2 data items
\node[thread] (t1) at (-5, 1.8) {Thread 1};
\node[thread] (t2) at (-5, 0.9) {Thread 2};
\node[thread] (t3) at (-5, 0.0) {Thread 3};
\node[thread] (t4) at (-5, -0.9) {Thread 4};

\node[op] (op1) at (-1.5, 1.8) {process};
\node[op] (op2) at (-1.5, 0.9) {process};
\node[op] (op3) at (-1.5, 0.0) {process};
\node[op] (op4) at (-1.5, -0.9) {process};

\draw[uses] (t1) -- (op1);
\draw[uses] (t2) -- (op2);
\draw[uses] (t3) -- (op3);
\draw[uses] (t4) -- (op4);

% Data assignment (data → thread)
\draw[fan] (d1.south) to[bend right=10] (t1.north);
\draw[fan] (d2.south) to[bend right=10] (t1.north);
\draw[fan] (d3.south) to[bend right=10] (t2.north);
\draw[fan] (d4.south) to[bend right=10] (t2.north);
\draw[fan] (d5.south) to[bend right=10] (t3.north);
\draw[fan] (d6.south) to[bend right=10] (t3.north);
\draw[fan] (d7.south) to[bend right=10] (t4.north);
\draw[fan] (d8.south) to[bend right=10] (t4.north);

\node[font=\sffamily\footnotesize\itshape, text=edgec] at (-3.5, -2.0)
    {같은 연산, 다른 데이터 (SPMD)};

% =========== RIGHT: Task Parallel ===========
\begin{scope}[xshift=12cm]
\node[font=\sffamily\bfseries\small] at (-3.5, 4.5) {Task Parallel};

% Input
\node[dcell, minimum width=22mm, minimum height=10mm,
      fill=absfill, draw=absbord] (img) at (-3.5, 3.4) {image};

% 3 threads, different operations on same data
\node[thread] (t1b) at (-6, 1.8) {Thread 1};
\node[op]     (o1b) at (-6, 0.6) {resize};

\node[thread] (t2b) at (-3.5, 1.8) {Thread 2};
\node[op]     (o2b) at (-3.5, 0.6) {filter};

\node[thread] (t3b) at (-1, 1.8) {Thread 3};
\node[op]     (o3b) at (-1, 0.6) {encode};

\draw[uses] (t1b) -- (o1b);
\draw[uses] (t2b) -- (o2b);
\draw[uses] (t3b) -- (o3b);

% Fan-out from img to threads
\draw[fan] (img.south) to[bend right=20] (t1b.north);
\draw[fan] (img.south) -- (t2b.north);
\draw[fan] (img.south) to[bend left=20] (t3b.north);

% Result
\node[dcell, minimum width=24mm, minimum height=10mm,
      fill=confill, draw=conbord] (res) at (-3.5, -0.8) {result};

% Fan-in
\draw[fan] (o1b.south) to[bend left=15] (res.north);
\draw[fan] (o2b.south) -- (res.north);
\draw[fan] (o3b.south) to[bend right=15] (res.north);

\node[font=\sffamily\footnotesize\itshape, text=edgec] at (-3.5, -2.0)
    {다른 연산, 파이프라인 단계};
\end{scope}

\end{tikzpicture}
\end{document}
