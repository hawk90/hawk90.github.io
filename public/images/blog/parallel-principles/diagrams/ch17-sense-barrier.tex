\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog]

\node[font=\sffamily\bfseries\large] at (6.5,3.6) {Sense-Reversing Barrier};

% Phase 1 (sense=false)
\node[abs, minimum width=58mm, minimum height=18mm, align=center] (p1)
  at (3.0, 1.4) {\textbf{Phase 1}\\\small global \texttt{sense=false}\\\small spinners: \texttt{wait until sense==false}};

% Trigger (last thread)
\node[hi, minimum width=58mm, minimum height=11mm, align=center] (t1)
  at (3.0,-1.0) {마지막 스레드: \texttt{sense $\leftarrow$ true}\\[1pt]\small (모두 풀려남)};

\draw[arr, line width=0.8pt] (p1.south) -- (t1.north);

% Phase 2 (sense=true)
\node[abs, minimum width=58mm, minimum height=18mm, align=center] (p2)
  at (10.0, 1.4) {\textbf{Phase 2}\\\small global \texttt{sense=true}\\\small spinners: \texttt{wait until sense==true}};

\node[hi, minimum width=58mm, minimum height=11mm, align=center] (t2)
  at (10.0,-1.0) {마지막 스레드: \texttt{sense $\leftarrow$ false}\\[1pt]\small (모두 풀려남)};

\draw[arr, line width=0.8pt] (p2.south) -- (t2.north);

% Transition between phases
\draw[arr, line width=1pt] (t1.east) -- ++(0.6,0) |- (p2.west)
  node[midway, above, caption] {다음 phase};

% Loop back arrow
\draw[arr, line width=1pt] (t2.east) -- ++(0.8,0) |- (4.4,3.0) -| (p1.north);

% Local sense annotation
\node[caption, anchor=center, align=center] at (6.5,-2.4)
  {각 스레드의 \texttt{local\_sense}도 phase마다 toggle하여 비교};

\end{tikzpicture}
\end{document}
