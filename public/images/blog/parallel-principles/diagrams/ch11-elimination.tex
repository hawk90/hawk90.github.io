\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{kotex}
\setmainhangulfont{Apple SD Gothic Neo}[BoldFont={Apple SD Gothic Neo Bold}]
\setsanshangulfont{Apple SD Gothic Neo}[BoldFont={Apple SD Gothic Neo Bold}]
\usetikzlibrary{positioning,arrows.meta,shapes,fit,calc}
\begin{document}
\input{../../_design.tex}
\input{../../_design-sequence.tex}
\begin{tikzpicture}[blog, seq, x=1cm, y=1cm]
  \node[actor]    (x) at (0, 0)  {Thread X};
  \node[actorAlt] (e) at (4, 0)  {Elimination};
  \node[actor]    (y) at (8, 0)  {Thread Y};

  \draw[lifeline] (x) -- ++(0,-6);
  \draw[lifeline] (e) -- ++(0,-6);
  \draw[lifeline] (y) -- ++(0,-6);

  \draw[msgsync] (x |- 0,-1.4) -- node[msglabel]{push(item) 대기} (e |- 0,-1.4);
  \draw[msgsync] (y |- 0,-2.4) -- node[msglabel]{pop 대기}        (e |- 0,-2.4);

  % match: dashed return on both sides
  \draw[msgret] (e |- 0,-3.4) -- node[msglabel]{매치 — X push 성공}    (x |- 0,-3.4);
  \draw[msgret] (e |- 0,-4.2) -- node[msglabel]{매치 — Y가 item 수신} (y |- 0,-4.2);

  \node[seqnote, anchor=north, text width=80mm, align=center]
       at ($(x)!.5!(y) + (0,-5.4)$)
       {만남 시점이 linearization point.\\push와 pop이 동시에 일어난 것으로 해석};
\end{tikzpicture}
\end{document}
