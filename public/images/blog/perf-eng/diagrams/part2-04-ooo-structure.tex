\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  stage/.style={rectangle, draw=nodebord, fill=#1, minimum width=28mm, minimum height=8mm, inner sep=3pt, font=\sffamily\footnotesize, align=center},
  unit/.style={rectangle, draw=impbord, fill=impfill, minimum width=10mm, minimum height=7mm, inner sep=2pt, font=\sffamily\scriptsize}
]

% Fetch
\node[font=\sffamily\footnotesize, text=inkc] (fetch) at (0, 0) {Fetch};

% Decode + Rename
\node[stage=absfill, draw=absbord] (decode) at (0, -1.2) {Decode + Rename};

% Issue Queue
\node[stage=clifill, draw=clibord] (issue) at (0, -2.6) {Issue Queue\\[2pt]\scriptsize (reservation station)};

% Execution Units
\node[unit] (alu0) at (-2.2, -4.2) {ALU0};
\node[unit] (alu1) at (-0.7, -4.2) {ALU1};
\node[unit] (lsu)  at (0.7, -4.2) {LSU};
\node[unit] (fpu)  at (2.2, -4.2) {FPU};

% Reorder Buffer
\node[stage=hifill, draw=hibord] (rob) at (0, -5.8) {Reorder Buffer\\[2pt]\scriptsize (in-order commit)};

% Architectural Register
\node[font=\sffamily\footnotesize, text=inkc] (arch) at (0, -7) {Architectural Register};

% Arrows
\draw[arr, line width=0.5pt] (fetch) -- (decode);
\draw[arr, line width=0.5pt] (decode) -- (issue);

% Issue to execution units
\draw[arr, line width=0.5pt] (issue.south) -- ++(0, -0.3) -| (alu0.north);
\draw[arr, line width=0.5pt] (issue.south) -- ++(0, -0.3) -| (alu1.north);
\draw[arr, line width=0.5pt] (issue.south) -- ++(0, -0.3) -| (lsu.north);
\draw[arr, line width=0.5pt] (issue.south) -- ++(0, -0.3) -| (fpu.north);

% Execution units to ROB
\draw[arr, line width=0.5pt] (alu0.south) |- ([yshift=5mm]rob.north) -- (rob.north);
\draw[arr, line width=0.5pt] (alu1.south) |- ([yshift=5mm]rob.north);
\draw[arr, line width=0.5pt] (lsu.south) |- ([yshift=5mm]rob.north);
\draw[arr, line width=0.5pt] (fpu.south) |- ([yshift=5mm]rob.north);

\draw[arr, line width=0.5pt] (rob) -- (arch);

% Label for parallel execution
\node[font=\sffamily\scriptsize, text=mutec, anchor=west] at (3, -4.2) {병렬 실행};

% Caption
\node[font=\sffamily\footnotesize, text=mutec, align=left, anchor=north west] at (-3.5, -7.8) {%
Issue 순서와 완료 순서가 다름\\[2pt]
ROB가 in-order commit을 보장};

\end{tikzpicture}
\end{document}
