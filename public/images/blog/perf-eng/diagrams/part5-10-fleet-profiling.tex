\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  device/.style={rectangle, rounded corners=2pt, draw=absbord, fill=absfill,
    minimum width=22mm, minimum height=8mm, font=\sffamily\footnotesize, align=center, line width=0.4pt},
  agent/.style={rectangle, rounded corners=2pt, draw=impbord, fill=impfill,
    minimum width=18mm, minimum height=7mm, font=\sffamily\scriptsize, align=center, line width=0.35pt},
  server/.style={rectangle, rounded corners=3pt, draw=hibord, fill=hifill,
    minimum width=28mm, minimum height=12mm, font=\sffamily\footnotesize\bfseries, align=center, line width=0.5pt}
]

% Title
\node[font=\sffamily\bfseries\small, text=inkc] at (3.5, 4.0) {Fleet Continuous Profiling — Parca 예시};

% Devices
\node[device] (d1) at (0, 2.5) {Device 1};
\node[device] (d2) at (0, 1.3) {Device 2};
\node[device] (dn) at (0, 0.1) {Device N};
\node[font=\sffamily\footnotesize, text=mutec] at (0, 0.8) {$\vdots$};

% Agents
\node[agent] (a1) at (2.5, 2.5) {Parca agent};
\node[agent] (a2) at (2.5, 1.3) {Parca agent};
\node[agent] (an) at (2.5, 0.1) {Parca agent};

% Central collector
\node[server] (col) at (5.5, 1.3) {중앙 collector};

% Alarm
\node[rectangle, rounded corners=2pt, draw=conbord, fill=confill,
      minimum width=16mm, minimum height=8mm, font=\sffamily\footnotesize, line width=0.4pt] (alarm) at (8.0, 1.3) {알람};

% Connections device -> agent
\draw[arr] (d1.east) -- (a1.west);
\draw[arr] (d2.east) -- (a2.west);
\draw[arr] (dn.east) -- (an.west);

% Connections agent -> collector
\draw[arr] (a1.east) -- (col.west |- a1.east);
\draw[arr] (a2.east) -- (col.west);
\draw[arr] (an.east) -- (col.west |- an.east);

% Collector -> alarm
\draw[arr] (col.east) -- (alarm.west);

% Annotation
\node[note, anchor=north, text width=65mm] at (3.5, -1.0) {
  \textbf{24/7 sampling, 1\% overhead}\\[2pt]
  ARM aarch64 binary 제공.\\[1pt]
  게이트웨이 급 보드에 배포 가능 (30-50 MB).
};

\end{tikzpicture}
\end{document}
