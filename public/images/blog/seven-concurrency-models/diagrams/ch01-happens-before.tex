\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc,decorations.pathreplacing}
\begin{document}
\input{../../_design.tex}
\input{../../_design-sequence.tex}
\begin{tikzpicture}[blog, seq, x=1cm, y=1cm,
  op/.style={cls, draw=nodebord, fill=nodefill, minimum width=30mm, minimum height=7mm,
             font=\sffamily\footnotesize, inner sep=2pt},
  hi/.style={op, draw=hibord, fill=hifill, font=\sffamily\bfseries\footnotesize},
  hb/.style={-{Stealth[length=2.4mm]}, draw=impbord, line width=0.7pt}
]
  % Actors
  \node[actor]    (a) at (0, 0)  {Thread A};
  \node[actorAlt] (l) at (5, 0)  {monitor (this)};
  \node[actor]    (b) at (10, 0) {Thread B};

  \draw[lifeline] (a) -- ++(0,-8);
  \draw[lifeline] (l) -- ++(0,-8);
  \draw[lifeline] (b) -- ++(0,-8);

  % A: acquire, write, release
  \node[hi, anchor=center] at (a |- 0, -1.0) {acquire (synchronized)};
  \node[op,     anchor=center] at (a |- 0, -2.0) {write x = 1};
  \node[op,     anchor=center] at (a |- 0, -2.8) {write y = 2};
  \node[hi, anchor=center] at (a |- 0, -3.6) {release};

  % monitor pass (happens-before edge across threads)
  \draw[hb] (a |- 0,-3.6) -- node[msglabel, font=\sffamily\bfseries\footnotesize,
                                  text=impbord]{happens-before} (b |- 0,-4.5);

  % B: acquire same monitor, read sees A's writes
  \node[hi, anchor=center] at (b |- 0, -4.5) {acquire (synchronized)};
  \node[op,     anchor=center, draw=okbord, fill=okfill]
       at (b |- 0, -5.5) {read x $\to$ 1 \textcolor{impbord}{✓}};
  \node[op,     anchor=center, draw=okbord, fill=okfill]
       at (b |- 0, -6.3) {read y $\to$ 2 \textcolor{impbord}{✓}};
  \node[hi, anchor=center] at (b |- 0, -7.1) {release};

  % within-thread program order (vertical arrows on A)
  \draw[hb] (a |- 0,-1.4) -- (a |- 0,-1.7);
  \draw[hb] (a |- 0,-2.3) -- (a |- 0,-2.6);
  \draw[hb] (a |- 0,-3.1) -- (a |- 0,-3.3);

  % within-thread program order on B
  \draw[hb] (b |- 0,-4.9) -- (b |- 0,-5.2);
  \draw[hb] (b |- 0,-5.7) -- (b |- 0,-6.0);
  \draw[hb] (b |- 0,-6.6) -- (b |- 0,-6.8);

  % Side captions
  \node[font=\sffamily\footnotesize, text=mutec, anchor=west, text width=35mm]
       at ($(a) + (-3.5, -2.4)$) {release: 캐시 $\to$ 메모리 flush};
  \node[font=\sffamily\footnotesize, text=mutec, anchor=west, text width=35mm]
       at ($(b) + (1.5, -4.9)$) {acquire: 캐시 무효화 $\to$ 최신값};
\end{tikzpicture}
\end{document}
