\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{kotex}
\setmainhangulfont{Apple SD Gothic Neo}[BoldFont={Apple SD Gothic Neo Bold}]
\setsanshangulfont{Apple SD Gothic Neo}[BoldFont={Apple SD Gothic Neo Bold}]
\usetikzlibrary{positioning,arrows.meta,shapes,fit,calc}
\begin{document}
\input{../../_design.tex}
\input{../../_design-state.tex}
\begin{tikzpicture}[blog, state, x=1cm, y=1cm]
  % Initial
  \node[initial] (init) at (-1, 3) {};

  % read step
  \node[stateBox, stateAbs] (read) at (1.6, 3) {old $\leftarrow$ @atom};
  % compute new
  \node[stateBox, stateImp] (comp) at (5.4, 3) {new $\leftarrow$ (f old)};
  % CAS decision
  \node[choice, minimum size=14mm] (cas) at (9.2, 3) {CAS\\old$\to$new};
  % success
  \node[stateBox, stateOk] (ok) at (12.5, 4.6) {return new};
  % final
  \node[final] (fin) at (14.8, 4.6) {};

  % retry note
  \node[stateBox, stateWarn] (retry) at (5.4, 0.8) {retry};

  % Transitions
  \draw[trans] (init) -- (read);
  \draw[trans] (read) -- (comp);
  \draw[trans] (comp) -- (cas);
  \draw[trans] (cas) -- node[transLabel]{success} (ok);
  \draw[trans] (ok)  -- (fin);

  % Failure path — CAS down to retry, retry back to read
  \draw[transError] (cas) |- node[transLabel, near start]{fail\,/\,값이 바뀜} (retry);
  \draw[transError] (retry) -| (read);

  % Note about pure function requirement
  \node[note, anchor=north west, text width=70mm] at (-1, -0.6)
       {\textbf{f는 순수 함수여야 한다} — retry되면 여러 번 호출됨.\\[2pt]
        \texttt{println} / I/O / atom 변경 등 부수 효과 금지.};
\end{tikzpicture}
\end{document}
