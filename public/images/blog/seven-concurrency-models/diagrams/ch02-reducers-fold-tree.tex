\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{kotex}
\setmainhangulfont{Apple SD Gothic Neo}[BoldFont={Apple SD Gothic Neo Bold}]
\setsanshangulfont{Apple SD Gothic Neo}[BoldFont={Apple SD Gothic Neo Bold}]
\usetikzlibrary{positioning,arrows.meta,shapes,fit,calc,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog, x=1cm, y=1cm]

  % ───────── Level 0: input collection ─────────
  \node[panelTitle, anchor=south west] at (-1.0, 6.6) {입력};
  \node[neut, minimum width=120mm, minimum height=8mm] (input) at (5.5, 6.0)
    {\texttt{[1\ 2\ 3\ 4\ 5\ 6\ 7\ 8\ \ldots]} \quad (vector, foldable)};

  % ───────── Level 1: split into 4 chunks ─────────
  \node[panelTitle, anchor=south west] at (-1.0, 4.6)
       {\textit{split} — 분할 (기본 임계값 512)};

  \node[clsSm, draw=absbord, fill=absfill, minimum width=22mm] (c1) at ( 0.0, 4.0) {chunk 1};
  \node[clsSm, draw=absbord, fill=absfill, minimum width=22mm] (c2) at ( 3.7, 4.0) {chunk 2};
  \node[clsSm, draw=absbord, fill=absfill, minimum width=22mm] (c3) at ( 7.4, 4.0) {chunk 3};
  \node[clsSm, draw=absbord, fill=absfill, minimum width=22mm] (c4) at (11.1, 4.0) {chunk 4};

  \draw[arr] ($(input.south) + (-5.5,0)$) -- (c1.north);
  \draw[arr] ($(input.south) + (-1.8,0)$) -- (c2.north);
  \draw[arr] ($(input.south) + ( 1.9,0)$) -- (c3.north);
  \draw[arr] ($(input.south) + ( 5.6,0)$) -- (c4.north);

  % ───────── Level 2: sequential reduce per chunk ─────────
  \node[panelTitle, anchor=south west] at (-1.0, 2.6)
       {\textit{reducef} — 부분 reduce (스레드별)};

  \node[imp, minimum width=22mm] (r1) at ( 0.0, 2.0) {reduce $\Rightarrow$ \texttt{a}};
  \node[imp, minimum width=22mm] (r2) at ( 3.7, 2.0) {reduce $\Rightarrow$ \texttt{b}};
  \node[imp, minimum width=22mm] (r3) at ( 7.4, 2.0) {reduce $\Rightarrow$ \texttt{c}};
  \node[imp, minimum width=22mm] (r4) at (11.1, 2.0) {reduce $\Rightarrow$ \texttt{d}};

  \draw[arr] (c1) -- (r1);
  \draw[arr] (c2) -- (r2);
  \draw[arr] (c3) -- (r3);
  \draw[arr] (c4) -- (r4);

  % ───────── Level 3: combine pairwise ─────────
  \node[panelTitle, anchor=south west] at (-1.0, 0.6)
       {\textit{combinef} — 결합 (페어 단위)};

  \node[hi, minimum width=28mm] (m1) at ( 1.85, 0.0) {combine $\Rightarrow$ \texttt{a$\oplus$b}};
  \node[hi, minimum width=28mm] (m2) at ( 9.25, 0.0) {combine $\Rightarrow$ \texttt{c$\oplus$d}};

  \draw[arr] (r1.south) -- (m1.north);
  \draw[arr] (r2.south) -- (m1.north);
  \draw[arr] (r3.south) -- (m2.north);
  \draw[arr] (r4.south) -- (m2.north);

  % ───────── Level 4: final combine ─────────
  \node[hi, minimum width=40mm, font=\sffamily\bfseries] (top) at (5.55, -2.0)
       {final $\Rightarrow$ \texttt{(a$\oplus$b)$\oplus$(c$\oplus$d)}};
  \draw[focus] (m1.south) -- (top.north);
  \draw[focus] (m2.south) -- (top.north);

  % Side note
  \node[note, text width=58mm, align=left, anchor=west]
    at (13.4, 1.0)
    {\textbf{조건}: $\oplus$가 결합법칙 만족.\\[2pt]
     \texttt{+, *, max, min,}\\
     \texttt{merge-with +} 가능.\\[2pt]
     비결합 연산(예: \texttt{-})은 불가.};

\end{tikzpicture}
\end{document}
