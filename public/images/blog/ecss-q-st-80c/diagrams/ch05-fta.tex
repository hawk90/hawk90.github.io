\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,shapes.gates.logic.US,shapes.geometric,positioning}
\usepackage{kotex}

\begin{document}
\input{../../_design.tex}

\begin{tikzpicture}[blog,
    topev/.style={rectangle, rounded corners=3pt, draw=conbord, fill=confill,
                  minimum width=58mm, minimum height=12mm, align=center,
                  font=\sffamily\small\bfseries},
    interm/.style={rectangle, rounded corners=2pt, draw=absbord, fill=absfill,
                   minimum width=32mm, minimum height=12mm, align=center,
                   font=\sffamily\scriptsize},
    basic/.style={circle, draw=impbord, fill=impfill, line width=0.5pt,
                  minimum size=18mm, align=center, font=\sffamily\scriptsize,
                  inner sep=1pt},
    ftagate/.style={draw=hibord, fill=hifill, line width=0.6pt,
                    rotate=90, transform shape,
                    minimum width=10mm, anchor=output},
    edge/.style={draw=edgec, line width=0.55pt}
]

% Top event
\node[topev] (top) at (0, 0) {Quaternion drift $>$ spec};

% OR gate below top
\node[or gate US, ftagate] (or) at (0, -2.0) {};
\node[font=\sffamily\scriptsize\bfseries, transform shape=false] at (or.center) {OR};

% Intermediate events (3) under OR
\node[interm] (e1) at (-4.2, -4.8) {Normalization\\infrequent};
\node[interm] (e2) at (0,    -4.8) {Integration error\\accumulate};
\node[interm] (e3) at (4.2,  -4.8) {Initial value\\wrong};

% AND gate under e1
\node[and gate US, ftagate] (and) at (-4.2, -6.8) {};
\node[font=\sffamily\scriptsize\bfseries, transform shape=false] at (and.center) {AND};

% Basic events under AND
\node[basic] (b1) at (-6.4, -9.4) {CR\\implemented};
\node[basic] (b2) at (-2.0, -9.4) {Math\\precision\\limit};

% Edges
\draw[edge] (top.south) -- (or.input 1);
\draw[edge] (top.south) -- (or.input 2);  % cosmetic, OR gate has 2 inputs

\draw[edge] (or.output) -- (e1.north);
\draw[edge] (or.output) -- (e2.north);
\draw[edge] (or.output) -- (e3.north);

\draw[edge] (e1.south) -- (and.input 1);
\draw[edge] (e1.south) -- (and.input 2);

\draw[edge] (and.output) -- (b1.north);
\draw[edge] (and.output) -- (b2.north);

\end{tikzpicture}
\end{document}
