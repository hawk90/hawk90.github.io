\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../../_design.tex}
\begin{tikzpicture}[blog,
  rowlab/.style={font=\sffamily\footnotesize, text=inkc, anchor=east},
  note/.style={font=\sffamily\footnotesize\itshape, text=mutec, anchor=west},
  clkLine/.style={draw=hibord, line width=0.9pt},
  dataLine/.style={draw=#1, line width=0.7pt}
]

% Section 1: before deskew
\node[font=\sffamily\bfseries\small, text=inkc, anchor=west] at (-2.0, 4.4) {초기 상태 (skew 존재)};

\foreach \i/\y/\name/\xoff/\note in {%
  0/3.8/fwd\_clk/0/{샘플 시점},
  1/3.0/{data[0]}/0/{정렬됨},
  2/2.2/{data[5]}/0.3/{$+0.3$ UI (늦음)},
  3/1.4/{data[12]}/-0.3/{$-0.3$ UI (빠름)}%
} {
  \node[rowlab] at (-0.2, \y) {\name};
  \ifnum\i=0
    \draw[clkLine] (0, \y) -- (7, \y);
    \foreach \tk in {1,2,3,4,5,6} {
      \draw[clkLine] (\tk, \y-0.12) -- (\tk, \y+0.12);
    }
  \else
    \draw[dataLine=conbord] (0, \y) -- (7, \y);
    \foreach \tk in {1,2,3,4,5,6} {
      \pgfmathsetmacro{\xt}{\tk + \xoff}
      \fill[conbord] (\xt, \y) circle (1.4pt);
    }
  \fi
  \node[note] at (7.1, \y) {\note};
}

% Section 2: after deskew
\node[font=\sffamily\bfseries\small, text=inkc, anchor=west] at (-2.0, 0.4) {deskew 후};

\foreach \i/\y/\name in {%
  0/-0.2/fwd\_clk,
  1/-1.0/{data[0]},
  2/-1.8/{data[5]},
  3/-2.6/{data[12]}%
} {
  \node[rowlab] at (-0.2, \y) {\name};
  \ifnum\i=0
    \draw[clkLine] (0, \y) -- (7, \y);
    \foreach \tk in {1,2,3,4,5,6} {
      \draw[clkLine] (\tk, \y-0.12) -- (\tk, \y+0.12);
    }
  \else
    \draw[dataLine=impbord] (0, \y) -- (7, \y);
    \foreach \tk in {1,2,3,4,5,6} {
      \fill[impbord] (\tk, \y) circle (1.4pt);
    }
  \fi
}
\node[note] at (7.1, -1.8) {모두 fwd\_clk과 정렬};

\end{tikzpicture}
\end{document}
