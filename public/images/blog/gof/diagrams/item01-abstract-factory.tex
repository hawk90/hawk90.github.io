\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog]

% --- Factory layer ---
\node[abs] (af) at (0, 4) {AbstractFactory \\ \scriptsize createButton()*  createWindow()*};
\node[con] (mf) at (-3.4, 1.6) {MacFactory};
\node[con] (wf) at ( 3.4, 1.6) {WinFactory};
\draw[inh] (mf.north) -- ++(0,0.4) -| (af.south);
\draw[inh] (wf.north) -- ++(0,0.4) -| (af.south);

% --- Product layer ---
\node[abs] (btn) at (-3, -1.4) {Button};
\node[abs] (win) at ( 3, -1.4) {Window};
\node[imp] (mb) at (-5,-3.6) {MacButton};
\node[imp] (wb) at (-1,-3.6) {WinButton};
\node[imp] (mw) at ( 1,-3.6) {MacWindow};
\node[imp] (ww) at ( 5,-3.6) {WinWindow};
\draw[inh] (mb.north) -- ++(0,0.4) -| (btn.south);
\draw[inh] (wb.north) -- ++(0,0.4) -| (btn.south);
\draw[inh] (mw.north) -- ++(0,0.4) -| (win.south);
\draw[inh] (ww.north) -- ++(0,0.4) -| (win.south);

% --- Returns ---
\draw[ret] (mf) to[bend right=15] node[left, font=\tiny]{creates} (mb.west);
\draw[ret] (mf) to[bend left=8]   (mw.north west);
\draw[ret] (wf) to[bend left=15]  node[right, font=\tiny]{creates} (ww.east);
\draw[ret] (wf) to[bend right=8]  (wb.north east);

% --- Legend (placed below the product row to avoid overlap) ---
\matrix[draw=groupbord, rounded corners, inner sep=4pt, row sep=1pt, column sep=4pt,
        font=\tiny, anchor=north west]
  at (-6.5, -5.0) {
    \node[abs, minimum width=12mm, minimum height=4mm]{}; & \node{abstract}; \\
    \node[con, minimum width=12mm, minimum height=4mm]{}; & \node{concrete factory}; \\
    \node[imp, minimum width=12mm, minimum height=4mm]{}; & \node{concrete product}; \\
    \draw[inh] (0,0) -- (8mm,0); & \node{inherits}; \\
    \draw[ret] (0,0) -- (8mm,0); & \node{creates / returns}; \\
  };
\end{tikzpicture}
\end{document}
