\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../_design.tex}
\begin{tikzpicture}[blog,
  pat/.style={cls, minimum height=7mm, minimum width=24mm},
  cre/.style={pat, draw=conbord, fill=confill},
  stp/.style={pat, draw=absbord, fill=absfill},
  bep/.style={pat, draw=impbord, fill=impfill}
]

% ============================================================
% Three regions stacked top-down — Creational, Structural, Behavioral
% Sparse intentional edges; legend on the right
% ============================================================

% ─── CREATIONAL ─────────────────────────────────────────────
\node[cre] (af)  at ( 0,    9) {Abstract\\Factory};
\node[cre] (bld) at ( 3.4,  9) {Builder};
\node[cre] (fm)  at ( 6.8,  9) {Factory\\Method};
\node[cre] (pt)  at (10.2,  9) {Prototype};
\node[cre] (sg)  at (13.6,  9) {Singleton};

\begin{scope}[on background layer]
  \node[group, fit=(af)(sg)] (gC) {};
\end{scope}
\node[glabel] at (gC.north west) {Creational};

% ─── STRUCTURAL ─────────────────────────────────────────────
\node[stp] (adp) at ( 0,    5.4) {Adapter};
\node[stp] (dec) at ( 3.4,  5.4) {Decorator};
\node[stp] (prx) at ( 6.8,  5.4) {Proxy};
\node[stp] (cmp) at (10.2,  5.4) {Composite};
\node[stp] (brd) at (13.6,  5.4) {Bridge};
\node[stp] (fcd) at ( 5.1,  3.6) {Facade};
\node[stp] (fly) at ( 8.5,  3.6) {Flyweight};

\begin{scope}[on background layer]
  \node[group, fit=(adp)(brd)(fcd)(fly)] (gS) {};
\end{scope}
\node[glabel] at (gS.north west) {Structural};

% ─── BEHAVIORAL ─────────────────────────────────────────────
\node[bep] (cor) at ( 0,    -0.3) {Chain of\\Responsibility};
\node[bep] (cmd) at ( 3.4,  -0.3) {Command};
\node[bep] (str_) at ( 6.8, -0.3) {Strategy};
\node[bep] (st)   at (10.2, -0.3) {State};
\node[bep] (tm)   at (13.6, -0.3) {Template\\Method};

\node[bep] (itr)  at ( 0,    -3.0) {Iterator};
\node[bep] (vis)  at ( 3.4,  -3.0) {Visitor};
\node[bep] (intr) at ( 6.8,  -3.0) {Interpreter};
\node[bep] (med)  at (10.2,  -3.0) {Mediator};
\node[bep] (obs)  at (13.6,  -3.0) {Observer};
\node[bep] (mem)  at ( 6.8,  -5.0) {Memento};

\begin{scope}[on background layer]
  \node[group, fit=(cor)(tm)(itr)(obs)(mem)] (gB) {};
\end{scope}
\node[glabel] at (gB.north west) {Behavioral};

% ============================================================
% RELATIONSHIPS — minimal & intentional
% ============================================================

% --- Creational internal ---
\draw[uses] (af) to[bend left=12] node[above, font=\tiny]{uses} (fm);
\draw[alt]  (af) to[bend left=15] (bld);
\draw[alt]  (af.south east) to[bend left=18] (pt.south west);

% --- Structural internal ---
\draw[alt] (adp) -- (dec);
\draw[alt] (dec) -- (prx);
\draw[alt] (cmp) -- (dec);
\draw[alt] (brd.south) to[bend right=15] (adp.south);

% --- Behavioral internal ---
\draw[alt]  (st)   -- (str_);
\draw[alt]  (str_) -- (tm);
\draw[uses] (cmd.south) to[bend right=18] node[right, font=\tiny]{uses} (mem.north);
\draw[uses] (med) to[bend right=15] node[above, sloped, font=\tiny]{uses} (obs);
\draw[uses] (vis) to[bend left=10]  node[above, sloped, font=\tiny]{visits} (intr);
\draw[uses] (cor) to[bend right=10] node[above, font=\tiny]{passes} (cmd);

% --- Cross-category ---
\draw[uses] (bld.south) to[bend right=20] node[left, font=\tiny]{builds} (cmp.north);
\draw[uses] (cmp.south) to[bend left=8]   node[right, font=\tiny]{traversed} (itr.north);
\draw[uses] (cmp.south) to[bend left=22]  node[right, font=\tiny]{visited}   (vis.north);
\draw[alt]  (fly.south) to[bend right=10] node[right, font=\tiny]{shared as} (str_.north);

% ============================================================
% LEGEND — bottom-right corner, inside the canvas
% ============================================================
\node[draw=groupbord, rounded corners=4pt, inner sep=6pt, font=\footnotesize,
      anchor=north east, align=left, fill=black!2]
  at (15.4, -4.5) {%
    \textbf{Legend}\\[3pt]
    \tikz \draw[uses] (0,0) -- (10mm,0); \ uses / composes\\
    \tikz \draw[alt]  (0,0) -- (10mm,0); \ similar / alternative\\[4pt]
    \textcolor{conbord}{\rule{2mm}{2mm}} \ Creational (5)\\
    \textcolor{absbord}{\rule{2mm}{2mm}} \ Structural (7)\\
    \textcolor{impbord}{\rule{2mm}{2mm}} \ Behavioral (11)
  };
\end{tikzpicture}
\end{document}
