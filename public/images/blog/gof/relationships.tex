\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc,bending}
\usepackage{kotex}
\begin{document}
\input{../_design.tex}

\begin{tikzpicture}[blog,
    pat/.style={
      rectangle, rounded corners=1.5pt, draw=nodebord, line width=0.5pt,
      fill=nodefill, minimum height=8mm, minimum width=22mm,
      align=center, inner sep=3pt,
      font=\sffamily\small\bfseries
    },
    cre/.style={pat, draw=conbord, fill=confill},   % Creational — red
    stp/.style={pat, draw=absbord, fill=absfill},   % Structural — blue
    bep/.style={pat, draw=impbord, fill=impfill},   % Behavioral — green
    relarr/.style={-{Stealth[length=2.4mm]}, draw=edgec, line width=0.5pt},
    rellbl/.style={font=\sffamily\tiny\itshape,
                   fill=white, draw=white, line width=2pt,
                   inner sep=1.5pt, align=center,
                   text=black!75}
]

% ============================================================
% Layout follows GoF book (Gamma et al.) — Composite as central
% hub; Iterator/Flyweight/Visitor/Interpreter cluster around it;
% Memento at top; Decorator/Strategy left column; Command/CoR
% right column; Generative + Facade at bottom; Proxy/Adapter/
% Bridge isolated on the right.
% ============================================================

% --- Top cluster ---
\node[bep] (mem) at ( 2.4,  8.2) {Memento};
\node[bep] (itr) at ( 0.8,  6.0) {Iterator};
\node[cre] (bld) at (-6.6,  6.8) {Builder};

% --- Central hub + immediate neighbors ---
\node[stp] (cmp) at ( 0,    3.6) {Composite};
\node[stp] (dec) at (-7.4,  3.2) {Decorator};
\node[stp] (fly) at (-1.8,  1.4) {Flyweight};
\node[bep] (vis) at ( 3.2,  2.0) {Visitor};
\node[bep] (intp)at ( 0.6,  0.0) {Interpreter};

% --- Right column ---
\node[bep] (cmd) at ( 7.0,  5.0) {Command};
\node[bep, minimum width=32mm] (cor) at ( 7.4,  2.8)
  {Chain of Responsibility};

% --- Mid-lower ---
\node[bep] (str) at (-7.4, -0.6) {Strategy};
\node[bep] (st)  at (-1.8, -2.4) {State};
\node[bep] (med) at ( 3.4, -2.4) {Mediator};
\node[bep, minimum width=24mm] (obs) at ( 8.2, -2.4) {Observer};

% --- Bottom: generative cluster + Facade ---
\node[bep, minimum width=30mm] (tm)  at ( 0.4, -5.0) {Template Method};
\node[cre, minimum width=28mm] (fm)  at ( 5.6, -6.6) {Factory Method};
\node[cre] (pt)  at (-6.0, -6.6) {Prototype};
\node[cre, minimum width=28mm] (af)  at (-1.8, -7.6) {Abstract Factory};
\node[cre] (sg)  at (-6.0, -10.0) {Singleton};
\node[stp] (fcd) at ( 2.0, -10.0) {Facade};

% --- Isolated cluster (top-right, separated by clear gap) ---
\node[stp] (prx) at ( 9.0,  8.4) {Proxy};
\node[stp] (adp) at ( 9.0,  7.2) {Adapter};
\node[stp] (brd) at ( 9.0,  6.0) {Bridge};

% ============================================================
% RELATIONSHIPS — follows the book's arrow labels
% ============================================================

% --- Iterator → Memento  (saving state of iteration) ---
\draw[relarr] (itr.north) to[bend left=20]
  node[rellbl, sloped, pos=0.55]{saving state\\of iteration} (mem.west);

% --- Command → Memento  (avoiding hysteresis) ---
\draw[relarr] (cmd.north) to[bend left=22]
  node[rellbl, sloped, pos=0.55]{avoiding\\hysteresis} (mem.east);

% --- Iterator → Composite  (enumerating children) ---
\draw[relarr] (itr.south) --
  node[rellbl, pos=0.45]{enumerating\\children} (cmp.north);

% --- Builder → Composite  (creating composites) ---
\draw[relarr] (bld.east) to[bend left=8]
  node[rellbl, sloped, pos=0.5]{creating\\composites} (cmp.north west);

% --- Decorator → Composite  (adding responsibilities to objects) ---
\draw[relarr] (dec.east) to[bend left=8]
  node[rellbl, pos=0.5]{adding responsibilities\\to objects} (cmp.west);

% --- Decorator ↔ Strategy  (changing skin versus guts) ---
\draw[{Stealth[length=2.4mm]}-{Stealth[length=2.4mm]},
      draw=edgec, line width=0.5pt]
  (dec.south) to[bend right=2]
    node[rellbl, sloped, pos=0.5]{changing skin\\versus guts} (str.north);

% --- Flyweight → Composite  (sharing composites) ---
\draw[relarr] (fly.north) to[bend right=8]
  node[rellbl, sloped, pos=0.55]{sharing\\composites} (cmp.south west);

% --- Flyweight → Strategy  (sharing strategies) ---
\draw[relarr] (fly.west) to[bend right=4]
  node[rellbl, sloped, pos=0.45]{sharing\\strategies} (str.east);

% --- Flyweight → State  (sharing states) ---
\draw[relarr] (fly.south) to[bend right=10]
  node[rellbl, sloped, pos=0.5]{sharing\\states} (st.north);

% --- Flyweight → Interpreter  (sharing terminal symbols) ---
\draw[relarr] (fly.south east) to[bend left=10]
  node[rellbl, sloped, pos=0.55]{sharing terminal\\symbols} (intp.north west);

% --- Visitor → Composite  (defining traversals) ---
\draw[relarr] (vis.north west) to[bend left=10]
  node[rellbl, sloped, pos=0.5]{defining\\traversals} (cmp.east);

% --- Visitor → Composite  (adding operations) — second relation ---
\draw[relarr] (vis.west) to[bend left=-4]
  node[rellbl, sloped, pos=0.5]{adding\\operations} (cmp.south east);

% --- Interpreter → Composite  (defining grammar) ---
\draw[relarr] (intp.north) to[bend left=12]
  node[rellbl, sloped, pos=0.55]{defining\\grammar} (cmp.south);

% --- Interpreter → Visitor  (adding operations) ---
\draw[relarr] (intp.east) to[bend right=14]
  node[rellbl, sloped, pos=0.55]{adding\\operations} (vis.south);

% --- Command → Composite  (composed using) — long curve ---
\draw[relarr] (cmd.west) to[bend right=12]
  node[rellbl, sloped, pos=0.55]{composed\\using} (cmp.north east);

% --- CoR → Composite  (defining the chain) — long curve ---
\draw[relarr] (cor.west) to[bend right=20]
  node[rellbl, sloped, pos=0.55]{defining\\the chain} (cmp.east);

% --- Mediator ↔ Observer  (complex dependency management) ---
\draw[{Stealth[length=2.4mm]}-{Stealth[length=2.4mm]},
      draw=edgec, line width=0.5pt]
  (med.east) -- (obs.west)
    node[rellbl, midway]{complex dependency\\management};

% --- Strategy → Template Method  (defining algorithm's steps) ---
\draw[relarr] (str.south) to[bend right=22]
  node[rellbl, sloped, pos=0.55]{defining algorithm's\\steps} (tm.west);

% --- Template Method → Factory Method  (often uses) ---
\draw[relarr] (tm.east) to[bend right=12]
  node[rellbl, pos=0.55]{often uses} (fm.north);

% --- Abstract Factory → Prototype  (configure factory dynamically) ---
\draw[relarr] (af.west) to[bend left=8]
  node[rellbl, pos=0.5]{configure factory\\dynamically} (pt.south);

% --- Abstract Factory → Factory Method  (implement using) ---
\draw[relarr] (af.east) to[bend right=8]
  node[rellbl, pos=0.5]{implement\\using} (fm.south);

% --- Abstract Factory → Singleton  (single instance) ---
\draw[relarr] (af.south west) to[bend left=14]
  node[rellbl, sloped, pos=0.55]{single\\instance} (sg.north east);

% --- Facade → Singleton  (single instance) ---
\draw[relarr] (fcd.west) -- (sg.east)
  node[rellbl, midway, above]{single instance};

% ============================================================
% Title + legend
% ============================================================
\node[font=\sffamily\bfseries\Large, anchor=south]
  at (1.0, -11.6) {Design Pattern Relationships};

\node[draw=groupbord, rounded corners=4pt, inner sep=5pt, font=\sffamily\scriptsize,
      anchor=north east, align=left, fill=black!2]
  at (10.6, -5.0) {%
    \textbf{Legend}\\[2pt]
    \textcolor{conbord}{\rule{2mm}{2mm}} \ Creational (5)\\
    \textcolor{absbord}{\rule{2mm}{2mm}} \ Structural (7)\\
    \textcolor{impbord}{\rule{2mm}{2mm}} \ Behavioral (11)\\[2pt]
    {\itshape →} \ uses\\
    {\itshape ↔} \ similar / alternative
  };

\end{tikzpicture}
\end{document}
