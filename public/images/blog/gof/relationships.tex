\documentclass[border=14pt,tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc,bending}
\usepackage{kotex}
\begin{document}
\input{../_design.tex}

\begin{tikzpicture}[blog,
    pat/.style={
      rectangle, rounded corners=2pt, draw=nodebord, line width=0.5pt,
      fill=nodefill, minimum height=9mm, minimum width=24mm,
      align=center, inner sep=3pt,
      font=\sffamily\small\bfseries
    },
    cre/.style={pat, draw=conbord, fill=confill},
    stp/.style={pat, draw=absbord, fill=absfill},
    bep/.style={pat, draw=impbord, fill=impfill},
    % --- arrows by relation type, colored for readability ---
    hubarr/.style={-{Stealth[length=2.6mm]},
                   draw=absbord, line width=0.6pt},   % into Composite hub (blue)
    sharearr/.style={-{Stealth[length=2.6mm]},
                     draw=impbord, line width=0.6pt}, % "sharing" from Flyweight (green)
    crearr/.style={-{Stealth[length=2.6mm]},
                   draw=conbord, line width=0.6pt},   % generative relations (red)
    relarr/.style={-{Stealth[length=2.6mm]},
                   draw=edgec, line width=0.55pt},    % other relations (dark gray)
    twoway/.style={{Stealth[length=2.6mm]}-{Stealth[length=2.6mm]},
                   draw=hibord, line width=0.7pt, dashed},  % alternatives (orange dashed)
    rellbl/.style={font=\sffamily\scriptsize,
                   fill=white, rounded corners=1.5pt,
                   draw=groupbord, line width=0.3pt,
                   inner sep=2.5pt, align=center,
                   text=black!82}
]

% ============================================================
% Wider canvas — readability first. Horizontal labels (no slope).
% Layout follows GoF book but spaced ~1.4× for label room.
% ============================================================

% --- Top row ---
\node[bep] (mem) at ( 3.2, 10.8) {Memento};
\node[bep] (itr) at ( 1.0,  8.0) {Iterator};
\node[cre] (bld) at (-9.0,  9.0) {Builder};

% --- Hub + immediate neighbors ---
\node[stp] (cmp) at ( 0,    4.6) {Composite};
\node[stp] (dec) at (-9.6,  4.4) {Decorator};
\node[stp] (fly) at (-2.4,  1.6) {Flyweight};
\node[bep] (vis) at ( 4.6,  2.8) {Visitor};
\node[bep] (intp)at ( 1.0, -0.6) {Interpreter};

% --- Right column (long curves into hub) ---
\node[bep] (cmd) at ( 9.6,  6.6) {Command};
\node[bep, minimum width=34mm] (cor) at (10.0,  3.8)
  {Chain of Responsibility};

% --- Mid-lower ---
\node[bep] (str) at (-9.6, -1.0) {Strategy};
\node[bep] (st)  at (-2.4, -3.4) {State};
\node[bep] (med) at ( 4.6, -3.6) {Mediator};
\node[bep] (obs) at (10.4, -3.6) {Observer};

% --- Bottom: generative cluster + Facade ---
\node[bep, minimum width=32mm] (tm)  at ( 0.6, -7.0) {Template Method};
\node[cre, minimum width=30mm] (fm)  at ( 7.4, -8.4) {Factory Method};
\node[cre] (pt)  at (-8.0, -8.4) {Prototype};
\node[cre, minimum width=30mm] (af)  at (-2.4, -9.6) {Abstract Factory};
\node[cre] (sg)  at (-7.6,-12.4) {Singleton};
\node[stp] (fcd) at ( 2.6,-12.4) {Facade};

% --- Isolated cluster (top-right) ---
\node[stp] (prx) at (12.6, 11.0) {Proxy};
\node[stp] (adp) at (12.6,  9.4) {Adapter};
\node[stp] (brd) at (12.6,  7.8) {Bridge};

% ============================================================
% RELATIONSHIPS — horizontal labels, color-coded by type
% ============================================================

% --- Top: Iterator/Command → Memento ---
\draw[relarr] (itr.north) to[bend left=15]
  node[rellbl, pos=0.5]{saving state\\of iteration} (mem.west);
\draw[relarr] (cmd.north) to[bend left=18]
  node[rellbl, pos=0.5]{avoiding\\hysteresis} (mem.east);

% --- Hub: things flowing into Composite (blue) ---
\draw[hubarr] (itr.south) --
  node[rellbl, pos=0.5]{enumerating\\children} (cmp.north);
\draw[hubarr] (bld.east) to[bend left=8]
  node[rellbl, pos=0.55]{creating\\composites} (cmp.north west);
\draw[hubarr] (dec.east) to[bend left=8]
  node[rellbl, pos=0.5]{adding\\responsibilities} (cmp.west);
% Two arrows from Visitor → Composite — strong vertical separation between
% the two labels so they don't share a row.
\draw[hubarr] (vis.north west) to[bend left=8]
  node[rellbl, pos=0.25, yshift=10pt]{defining\\traversals} (cmp.east);
\draw[hubarr] (vis.west) to[bend right=8]
  node[rellbl, pos=0.75, yshift=-10pt]{adding\\operations} (cmp.south east);
\draw[hubarr] (intp.north) to[bend left=10]
  node[rellbl, pos=0.55]{defining\\grammar} (cmp.south);
\draw[hubarr] (cmd.west) to[bend right=10]
  node[rellbl, pos=0.5]{composed\\using} (cmp.north east);
\draw[hubarr] (cor.west) to[bend right=15]
  node[rellbl, pos=0.5]{defining\\the chain} (cmp.east);

% --- Flyweight as sharing source (green) ---
\draw[sharearr] (fly.north) to[bend right=10]
  node[rellbl, pos=0.55]{sharing\\composites} (cmp.south west);
\draw[sharearr] (fly.west) --
  node[rellbl, pos=0.5]{sharing\\strategies} (str.east);
\draw[sharearr] (fly.south) to[bend right=10]
  node[rellbl, pos=0.5]{sharing\\states} (st.north);
\draw[sharearr] (fly.south east) to[bend left=8]
  node[rellbl, pos=0.55]{sharing terminal\\symbols} (intp.north west);

% --- Interpreter → Visitor ---
\draw[relarr] (intp.east) to[bend right=10]
  node[rellbl, pos=0.5]{adding\\operations} (vis.south);

% --- Alternatives (dashed two-way, orange) ---
\draw[twoway] (dec.south) -- (str.north)
  node[rellbl, midway]{changing skin\\versus guts};
\draw[twoway] (med.east) -- (obs.west)
  node[rellbl, midway]{complex dependency\\management};

% --- Generative relations (red) ---
\draw[crearr] (str.south) to[bend right=15]
  node[rellbl, pos=0.5]{defining algorithm's\\steps} (tm.west);
\draw[crearr] (tm.east) to[bend right=8]
  node[rellbl, pos=0.5]{often uses} (fm.north);
\draw[crearr] (af.west) to[bend left=8]
  node[rellbl, pos=0.5]{configure factory\\dynamically} (pt.south);
\draw[crearr] (af.east) to[bend right=8]
  node[rellbl, pos=0.5]{implement\\using} (fm.south);
\draw[crearr] (af.south west) to[bend right=10]
  node[rellbl, pos=0.5]{single\\instance} (sg.north east);
\draw[crearr] (fcd.west) --
  node[rellbl, pos=0.55]{single instance} (sg.east);

% ============================================================
% Title + legend
% ============================================================
\node[font=\sffamily\bfseries\Large, anchor=south]
  at (1.5, -14.2) {Design Pattern Relationships};

% Legend — moved to bottom-left (well below the diagram bottom at y≈-12.4)
% so it doesn't crash into the Builder node at (-9, 9)
\node[draw=groupbord, rounded corners=4pt, inner sep=6pt, font=\sffamily\footnotesize,
      anchor=north west, align=left, fill=black!2]
  at (-12.2, -13.6) {%
    \textbf{Category}\\[2pt]
    \textcolor{conbord}{\rule{3mm}{3mm}} \ Creational (5)\\
    \textcolor{absbord}{\rule{3mm}{3mm}} \ Structural (7)\\
    \textcolor{impbord}{\rule{3mm}{3mm}} \ Behavioral (11)\\[4pt]
    \textbf{Arrow color}\\[2pt]
    \textcolor{absbord}{\rule{6mm}{1pt}}$\to$ \ into Composite hub\\
    \textcolor{impbord}{\rule{6mm}{1pt}}$\to$ \ Flyweight sharing\\
    \textcolor{conbord}{\rule{6mm}{1pt}}$\to$ \ generative\\
    \textcolor{edgec}{\rule{6mm}{1pt}}$\to$ \ other uses\\
    \textcolor{hibord}{\rule{6mm}{0.8pt}} \ alternative ($\leftrightarrow$)
  };

\end{tikzpicture}
\end{document}
