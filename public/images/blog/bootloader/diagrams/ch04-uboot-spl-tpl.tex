\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog]

\node[font=\sffamily\bfseries\large] at (0,6.6) {U-Boot SPL / TPL 모델};

\node[neut, minimum width=82mm, minimum height=10mm, align=center] (rom) at (0,5.2)
  {BootROM};

\node[abs, minimum width=82mm, minimum height=14mm, align=center] (tpl) at (0,3.2)
  {\textbf{TPL — Tertiary Program Loader} (옵션)\\\small 매우 작은 SRAM (예: 12 KB)에서 동작 · SPL 적재};

\node[abs, minimum width=82mm, minimum height=14mm, align=center] (spl) at (0,1.0)
  {\textbf{SPL — Secondary Program Loader}\\\small DDR이 없는 SRAM에서 동작 · DDR init · U-Boot Proper 적재};

\node[imp, minimum width=82mm, minimum height=14mm, align=center] (upp) at (0,-1.2)
  {\textbf{U-Boot Proper}\\\small DDR에서 동작 · 명령 인터프리터 · 커널 적재};

\node[hi, minimum width=82mm, minimum height=10mm, align=center] (lk) at (0,-3.0)
  {Linux Kernel};

\draw[arr, line width=0.8pt] (rom.south) -- (tpl.north);
\draw[arr, line width=0.8pt] (tpl.south) -- (spl.north);
\draw[arr, line width=0.8pt] (spl.south) -- (upp.north);
\draw[arr, line width=0.8pt] (upp.south) -- (lk.north);

\end{tikzpicture}
\end{document}
