\documentclass[tikz,border=2mm]{standalone}
\input{../../_design.tex}

\begin{document}
\begin{tikzpicture}[blog]

% Aggregate boundary
\node[abs, text width=10cm, minimum height=7cm, align=center] (boundary) at (5.5, 4) {};
\node[text=conbord, font=\bfseries] at (5.5, 7.3) {Cargo Aggregate};

% Aggregate Root
\node[con, text width=8cm, align=center, minimum height=1cm] (root) at (5.5, 6) {
  \textbf{Cargo (Aggregate Root)}
};

% Internal entities
\node[imp, text width=3.5cm, align=center] (itinerary) at (3, 4) {Itinerary};
\node[imp, text width=3.5cm, align=center] (delivery) at (8, 4) {Delivery};
\node[cli, text width=3.5cm, align=center] (leg) at (3, 2.2) {Leg};

% Arrows
\draw[arr] (root.south) -| (itinerary.north);
\draw[arr] (root.south) -| (delivery.north);
\draw[arr] (itinerary.south) -- (leg.north);

% External access label
\node[prp, text width=9cm, align=center] at (5.5, 0.5) {
  외부에서는 Root만 접근
};

\end{tikzpicture}
\end{document}
