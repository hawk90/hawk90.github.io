\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc,decorations.pathreplacing}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog, x=12mm, y=6mm,
  sig/.style={draw=hibord, line width=1.4pt},
  marker/.style={draw=warnbord, line width=0.4pt, dashed},
  brace/.style={decorate, decoration={brace, amplitude=4pt, raise=2pt}, draw=edgec, line width=0.4pt},
  lbl/.style={font=\scriptsize, anchor=east},
  caption/.style={font=\scriptsize, anchor=south}
]

\node[font=\footnotesize\bfseries, anchor=south] at (3.5, 3.5)
  {GPIO debug pin — ISR $\to$ Scheduler $\to$ Task wake};

% GPIO line: low, then ISR pulse high, low gap (scheduler), high again at task start
\node[lbl] at (-0.3, 0.5) {DBG};
\draw[sig] (0, 0)         % low until t2
  -- (1.0, 0)
  -- (1.0, 1.4)            % t2: rise (ISR start)
  -- (3.0, 1.4)            % ISR running high
  -- (3.0, 0)              % t4: fall (ISR end)
  -- (4.0, 0)              % scheduler low gap
  -- (4.0, 1.4)            % t7: rise (task start)
  -- (5.5, 1.4)            % task running
  -- (5.5, 0)
  -- (6.5, 0);

% Time markers
\foreach \x/\name in {1.0/$t_2$, 3.0/$t_4$, 4.0/$t_7$} {
  \draw[marker] (\x, -0.4) -- (\x, 2.2);
  \node[font=\scriptsize, anchor=north] at (\x, -0.4) {\name};
}

% Phase brace: ISR width
\draw[brace] (1.0, 2.0) -- (3.0, 2.0);
\node[caption] at (2.0, 2.2) {\textbf{ISR}};

% Phase brace: Scheduler width
\draw[brace] (3.0, 2.0) -- (4.0, 2.0);
\node[caption] at (3.5, 2.2) {\textbf{Sched}};

% Phase brace: Task start
\draw[brace] (4.0, 2.0) -- (5.5, 2.0);
\node[caption] at (4.75, 2.2) {Task};

% Time axis baseline
\draw[draw=edgec, line width=0.4pt] (0, -0.4) -- (6.5, -0.4);
\node[font=\tiny, anchor=west] at (6.5, -0.4) {time};

% Annotations farther below to avoid t-marker labels
\node[font=\tiny, text=mutec, anchor=north] at (2.0, -1.3) {ISR width};
\node[font=\tiny, text=mutec, anchor=north] at (3.5, -1.3) {scheduler latency};
\node[font=\tiny, text=mutec, anchor=north] at (4.75, -1.3) {task running};

\end{tikzpicture}
\end{document}
