\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  hdr/.style={font=\sffamily\bfseries\small, text=inkc, align=center},
  subhdr/.style={font=\sffamily\bfseries\footnotesize, text=mutec, align=center},
  region/.style={rectangle, draw=nodebord, line width=0.45pt, minimum width=42mm, minimum height=12mm, inner sep=3pt, font=\sffamily\footnotesize, align=center, fill=white},
  secreg/.style={region, draw=warnbord, fill=warnfill},
  nsreg/.style={region, draw=absbord, fill=absfill},
  nscreg/.style={region, draw=hibord, fill=hifill, font=\sffamily\bfseries\footnotesize, align=center},
  fw/.style={rectangle, rounded corners=2pt, draw=impbord, line width=0.45pt, fill=impfill, minimum width=42mm, minimum height=9mm, inner sep=2pt, font=\sffamily\footnotesize, align=center},
  fwns/.style={fw, draw=conbord, fill=confill}
]

% ===== Title =====
\node[hdr] at (0, 8.0) {TrustZone for Armv8-M — 두 세계, 한 코어};

% ============================================================
% LEFT panel: Secure World
% ============================================================
\node[hdr, text=warnbord] at (-5.0, 7.0) {Secure World (S)};

\node[fw] (sfw) at (-5.0, 5.8) {TF-M / Secure Firmware};
\node[secreg, below=4mm of sfw] (sflash) {Secure Flash \\[2pt] \footnotesize \texttt{0x0C00\_0000}};
\node[secreg, below=4mm of sflash] (ssram) {Secure SRAM \\[2pt] \footnotesize \texttt{0x3000\_0000}};
\node[secreg, below=4mm of ssram] (speri) {Secure Peripherals \\[2pt] \footnotesize TRNG, Crypto, AHB};

% ============================================================
% MIDDLE: NSC veneer
% ============================================================
\node[nscreg, minimum width=26mm, minimum height=26mm] (nsc) at (0, 2.4) {
  \textbf{NSC} \\[4pt]
  Veneer \\[4pt]
  \texttt{SG} 명령 \\[6pt]
  \footnotesize \texttt{0x0C03\_8000}
};

\node[subhdr, anchor=south, yshift=2pt] at (nsc.north) {Non-Secure Callable};

% ============================================================
% RIGHT panel: Non-Secure World
% ============================================================
\node[hdr, text=absbord] at (5.0, 7.0) {Non-Secure World (NS)};

\node[fwns] (nsfw) at (5.0, 5.8) {FreeRTOS / Zephyr + App};
\node[nsreg, below=4mm of nsfw] (nsflash) {Non-Secure Flash \\[2pt] \footnotesize \texttt{0x0800\_0000}};
\node[nsreg, below=4mm of nsflash] (nssram) {Non-Secure SRAM \\[2pt] \footnotesize \texttt{0x2000\_0000}};
\node[nsreg, below=4mm of nssram] (nsperi) {NS Peripherals \\[2pt] \footnotesize GPIO, UART, SPI};

% ============================================================
% Cross-world call flow arrows — keep simple, away from text
% ============================================================
% NS -> NSC (call into secure)
\draw[focus, line width=0.7pt, ->] (nsfw.west) -- ($(nsc.east)+(0,0.6)$)
  node[midway, above, font=\sffamily\scriptsize, text=focusc] {NS$\to$NSC};

% NSC -> Secure (after SG)
\draw[focus, line width=0.7pt, ->] (nsc.west) -- (sfw.east)
  node[midway, above, font=\sffamily\scriptsize, text=focusc] {SG $\to$ S};

% Secure -> NS (return via BXNS) — below
\draw[arr, dashed, draw=mutec, line width=0.5pt] (ssram.east) -- ($(nsc.west)+(0,-0.6)$);
\draw[arr, dashed, draw=mutec, line width=0.5pt] ($(nsc.east)+(0,-0.6)$) -- (nssram.west)
  node[midway, below, font=\sffamily\scriptsize, text=mutec] {BXNS return};

% ============================================================
% Bottom caption
% ============================================================
\node[caption, text width=16cm, anchor=north, align=center] at (0, -1.2) {
  \textbf{SAU}(software) + \textbf{IDAU}(hardware)가 각 region에 S / NS / NSC attribute를 부여.\\[3pt]
  더 secure한 쪽이 적용. NS는 NSC veneer 없이 Secure로 진입 불가.
};

\end{tikzpicture}
\end{document}
