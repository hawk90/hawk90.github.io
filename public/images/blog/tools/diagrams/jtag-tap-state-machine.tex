\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes}
\usepackage{kotex}

\begin{document}
\input{../../_design.tex}

\begin{tikzpicture}[blog,
    state/.style={rectangle, rounded corners=2pt, draw=absbord, fill=absfill,
                  minimum width=2.6cm, minimum height=0.7cm, align=center,
                  font=\sffamily\footnotesize},
    drstate/.style={state, draw=conbord, fill=confill},
    irstate/.style={state, draw=impbord, fill=impfill},
    trans/.style={-{Stealth[length=1.8mm]}, draw=edgec, line width=0.4pt,
                  font=\sffamily\scriptsize}
]

% Top common states (centered, vertical)
\node[state] (reset) at (0, 5.0) {Test-Logic-Reset};
\node[state] (idle)  at (0, 3.8) {Run-Test/Idle};
\node[state] (seldr) at (0, 2.6) {Select-DR-Scan};
\node[state] (selir) at (0, 1.4) {Select-IR-Scan};

% Top transitions
\draw[trans] (reset) -- node[right] {TMS=0} (idle);
\draw[trans] (idle)  -- node[right] {TMS=1} (seldr);
\draw[trans] (seldr) -- node[right] {TMS=1} (selir);

% DR branch (left column) — well-separated
\node[drstate] (capdr)  at (-5, 1.4) {Capture-DR};
\node[drstate] (shftdr) at (-5, 0.2) {Shift-DR};
\node[drstate] (ex1dr)  at (-5, -1.0) {Exit1-DR};
\node[drstate] (upddr)  at (-5, -2.2) {Update-DR};

\draw[trans] (seldr.west) to[bend right=15]
    node[above, font=\sffamily\scriptsize, sloped] {TMS=0}
    (capdr.north);
\draw[trans] (capdr)  -- node[left] {TMS=0} (shftdr);
\draw[trans] (shftdr) -- node[left] {TMS=1} (ex1dr);
\draw[trans] (ex1dr)  -- node[left] {TMS=1} (upddr);

% IR branch (right column)
\node[irstate] (capir)  at (5, 0.2) {Capture-IR};
\node[irstate] (shftir) at (5, -1.0) {Shift-IR};
\node[irstate] (ex1ir)  at (5, -2.2) {Exit1-IR};
\node[irstate] (updir)  at (5, -3.4) {Update-IR};

\draw[trans] (selir.east) to[bend left=15]
    node[above, font=\sffamily\scriptsize, sloped] {TMS=0}
    (capir.north);
\draw[trans] (capir)  -- node[right] {TMS=0} (shftir);
\draw[trans] (shftir) -- node[right] {TMS=1} (ex1ir);
\draw[trans] (ex1ir)  -- node[right] {TMS=1} (updir);

% Self-loop annotation (textual, no curve)
\node[font=\sffamily\scriptsize, text=conbord, anchor=east]
    at ([xshift=-0.3cm]shftdr.west) {Shift loop (TMS=0)};
\node[font=\sffamily\scriptsize, text=impbord, anchor=west]
    at ([xshift=0.3cm]shftir.east) {Shift loop (TMS=0)};

% Bottom note: data direction
\node[font=\sffamily\scriptsize, text=hibord, align=center, anchor=north]
    at (0, -4.0)
    {Shift-DR / Shift-IR 상태에서 TDI $\rightarrow$ TDO로 비트 시프트};

\end{tikzpicture}
\end{document}
