\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  bstep/.style={cls, minimum width=58mm, minimum height=10mm, align=center},
  boot/.style={bstep, draw=conbord, fill=confill},
  init/.style={bstep, draw=clibord, fill=clifill},
  cppi/.style={bstep, draw=prpbord, fill=prpfill},
  appf/.style={bstep, draw=impbord, fill=impfill, font=\sffamily\bfseries\small}
]

\node[boot] (reset) at (0, 8) {1. Reset\_Handler};
\node[boot, below=4mm of reset] (data) {2. .data 복사 (Flash $\to$ RAM)};
\node[boot, below=4mm of data] (bss) {3. .bss 0 초기화};
\node[init, below=4mm of bss] (sysinit) {4. SystemInit (clock, FPU)};
\node[cppi, below=4mm of sysinit] (libc) {5. \_\_libc\_init\_array};

\node[cppi, right=18mm of libc, minimum width=48mm, align=left, font=\sffamily\footnotesize] (detail) {
  $\bullet$ .preinit\_array \\
  $\bullet$ \_init() \\
  $\bullet$ .init\_array $\to$ C++ 생성자
};

\node[appf, below=4mm of libc] (main) {6. main()};

% Arrows
\draw[flow] (reset) -- (data);
\draw[flow] (data) -- (bss);
\draw[flow] (bss) -- (sysinit);
\draw[flow] (sysinit) -- (libc);
\draw[uses, dashed, draw=prpbord] (libc.east) -- (detail.west);
\draw[focus] (libc) -- (main);

% Annotations
\node[caption, above=2mm of reset] {전원 ON / Reset 핀};
\node[font=\sffamily\footnotesize, anchor=west, text=mutec] at (3.5, 5.6) {(어셈블리 또는 C)};
% C++ runtime 초기화 라벨은 detail 박스 아래로
\node[font=\sffamily\footnotesize, anchor=north west, text=mutec] at ($(detail.south west)+(0,-2mm)$) {(C++ runtime 초기화)};
\node[font=\sffamily\bfseries\footnotesize, text=focusc, anchor=west] at (4.0, 1.0) {여기서 C++ 객체 사용 가능};

\end{tikzpicture}
\end{document}
