\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  cell/.style={rectangle, draw=nodebord, line width=0.4pt, minimum width=12mm, minimum height=10mm, inner sep=2pt, font=\sffamily\footnotesize, align=center},
  used/.style={cell, fill=clifill, draw=clibord},
  freeC/.style={cell, fill=white},
  ptr/.style={font=\sffamily\bfseries\footnotesize, text=focusc}
]

% 8-slot ring (top half: linear)
\foreach \i [count=\xi] in {0,...,7} {
  \pgfmathsetmacro{\fillcolor}{(\i>=2 && \i<=5) ? "used" : "freeC"}
  \ifnum\i<2
    \node[freeC] (c\i) at (\i*1.3, 0) {};
  \else
    \ifnum\i>5
      \node[freeC] (c\i) at (\i*1.3, 0) {};
    \else
      \node[used] (c\i) at (\i*1.3, 0) {Data};
    \fi
  \fi
  \node[font=\sffamily\scriptsize, text=mutec, below=2pt of c\i] {\i};
}

% Tail pointer (consumer)
\node[ptr] (tail) at (2*1.3, 1.4) {tail (consumer)};
\draw[arr, draw=focusc] (tail.south) -- (c2.north);

% Head pointer (producer)
\node[ptr] (head) at (6*1.3, 1.4) {head (producer)};
\draw[arr, draw=focusc] (head.south) -- (c6.north);

% Title
\node[font=\sffamily\bfseries\small] at (4.5, 2.4) {SPSC Ring Buffer (capacity 8)};

% Bottom: explanation
\node[font=\sffamily\footnotesize, text=conbord, anchor=west] at (0, -1.5) {Consumer: tail $\to$ data $\to$ tail++};
\node[font=\sffamily\footnotesize, text=impbord, anchor=west] at (0, -2.1) {Producer: head++ $\to$ data write};
\node[font=\sffamily\bfseries\footnotesize, text=focusc, anchor=west, text width=11cm] at (0, -2.9) {Producer는 head만, Consumer는 tail만 수정 — atomic + acquire/release만으로 lock-free};

\end{tikzpicture}
\end{document}
