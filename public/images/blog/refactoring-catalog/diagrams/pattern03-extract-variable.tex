\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog]

\node[font=\sffamily\bfseries] at (-4.5, 2.6) {Before};
\node[con, text width=50mm, align=left, minimum height=16mm] (big) at (-4.5, 1)
  {return order.qty * order.price\\\quad - max(0, order.qty - 500)\\\qquad * order.price * 0.05;};

\node[font=\sffamily\bfseries] at (4.5, 2.6) {After};
\node[imp, text width=50mm, align=left, minimum height=8mm] (b) at (4.5, 2)
  {basePrice = order.qty * order.price;};
\node[imp, text width=50mm, align=left, minimum height=8mm] (q) at (4.5, 0.8)
  {qtyDiscount = max(0, ...) * 0.05;};
\node[hi, text width=50mm, align=left, minimum height=8mm] (r) at (4.5, -0.4)
  {return basePrice - qtyDiscount;};
\draw[arr] (b.south) -- (q.north);
\draw[arr] (q.south) -- (r.north);

\draw[->, edgec, line width=1.2pt] (-1.7, 1) -- node[above, font=\footnotesize]{Extract Variable} (1.8, 0.8);

\end{tikzpicture}
\end{document}
