\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog]

\node[font=\sffamily\bfseries] at (-4.5, 3) {Before};
\node[con, text width=50mm, align=left, minimum height=22mm] (b) at (-4.5, 1.2)
  {if (date.before(SUMMER\_START)\\\quad || date.after(SUMMER\_END))\\\quad charge = qty * winterRate + fee;\\else\\\quad charge = qty * summerRate;};

\node[font=\sffamily\bfseries] at (4.5, 3) {After};
\node[imp, text width=50mm, align=left, minimum height=22mm] (a) at (4.5, 1.2)
  {if (notSummer(date))\\\quad charge = winterCharge(qty);\\else\\\quad charge = summerCharge(qty);};

\draw[->, edgec, line width=1.2pt] (-1.7, 1.2) -- node[above, font=\footnotesize]{Decompose Conditional} (1.6, 1.2);

\node[font=\footnotesize\itshape, mutec, text width=80mm, align=center] at (0, -1.4)
  {결정과 분기 결과를 named function으로};

\end{tikzpicture}
\end{document}
