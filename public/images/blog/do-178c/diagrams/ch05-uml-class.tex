\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{pgf-umlcd}
\usepackage{kotex}

% Color tweaks — match blog palette
\renewcommand{\umltextcolor}{black}
\renewcommand{\umldrawcolor}{black!60}
\renewcommand{\umlfillcolor}{blue!4}

\begin{document}

\begin{tikzpicture}[font=\sffamily\small]

% ISensor — interface, top-left
\begin{interface}[text width=4.2cm]{ISensor}{0, 0}
  \operation{+ read() : Data}
\end{interface}

% GpsReceiver — concrete, top-right
\begin{class}[text width=4.4cm]{GpsReceiver}{6.5, 0}
  \implement{ISensor}
  \operation{+ read() : Data}
  \operation{+ calibrate()}
\end{class}

% ImuReceiver — concrete, below ISensor
\begin{class}[text width=4.4cm]{ImuReceiver}{0, -3.8}
  \implement{ISensor}
  \operation{+ read() : Data}
  \operation{+ \ldots}
\end{class}

\end{tikzpicture}
\end{document}
