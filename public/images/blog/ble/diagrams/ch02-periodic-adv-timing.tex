\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc,decorations.pathreplacing}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog, x=12mm, y=5mm,
  axis/.style={draw=edgec, line width=0.4pt},
  pulse/.style={fill=hifill, draw=hibord, line width=0.8pt},
  brace/.style={decorate, decoration={brace, amplitude=4pt, raise=2pt}, draw=edgec, line width=0.4pt},
  marker/.style={draw=warnbord, line width=0.4pt, dashed}
]

\node[font=\footnotesize\bfseries, anchor=south] at (4.5, 4.5)
  {Periodic Advertising — fixed-interval beacons (BLE 5.0)};

% Time axis
\draw[axis] (0, 1.0) -- (9.5, 1.0);

% Pulses (periodic adv packets) at uniform interval T
\foreach \x in {0.5, 1.7, 2.9, 4.1, 5.3, 6.5, 7.7, 8.9} {
  \fill[pulse] (\x, 0.8) rectangle (\x+0.2, 1.8);
}

% Tick labels (T) below axis with clear separation
\foreach \x in {0.5, 1.7, 2.9, 4.1, 5.3, 6.5, 7.7, 8.9} {
  \node[font=\tiny, anchor=north, text=mutec] at (\x+0.1, 0.85) {T};
}

% Interval brace (above first two pulses)
\draw[brace] (0.7, 2.2) -- (1.9, 2.2);
\node[font=\scriptsize, anchor=south] at (1.3, 2.7) {fixed interval};

% Description box separated below (extra line spacing to avoid glyph collision)
\node[font=\scriptsize, anchor=north west, align=left, text=mutec]
  at (0, -0.5) {
  \textbullet\ 수신자는 다음 packet 시각을 \textit{미리} 안다 — interval 사이엔 sleep\\[3pt]
  \textbullet\ 광고처럼 무작위 advDelay 없음 — 결정적 (deterministic) 타이밍\\[3pt]
  \textbullet\ Auracast / broadcast audio 등 동기 수신이 핵심인 곳에 사용
};

\end{tikzpicture}
\end{document}
