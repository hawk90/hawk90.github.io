\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog]

% Title
\node[font=\sffamily\bfseries\large] at (0,5.2) {Responsibility Separation (Before / After)};

% --- BEFORE ---
\node[font=\sffamily\bfseries] at (0,4.2) {Before — Main 한 곳에 모든 책임};

\node[con, minimum width=80mm, minimum height=24mm, align=left] (before) at (0,2.5)
  {\textbf{Main}\\[3pt]
   $\bullet$ XMPP 연결\\
   $\bullet$ UI 생성\\
   $\bullet$ Sniper 생성\\
   $\bullet$ 이벤트 처리\\
   $\bullet$ 메시지 전송};

% --- AFTER ---
\node[font=\sffamily\bfseries] at (0,0.2) {After — 세 클래스로 분리};

\node[hi,  minimum width=32mm, minimum height=22mm, align=left] (m)      at (-4.5,-2.0)
  {\textbf{Main}\\[2pt]
   $\bullet$ 시작점\\
   $\bullet$ 조립};

\node[abs, minimum width=36mm, minimum height=22mm, align=left] (house)  at (0,-2.0)
  {\textbf{AuctionHouse}\\[2pt]
   $\bullet$ XMPP 연결\\
   $\bullet$ Auction 생성};

\node[imp, minimum width=36mm, minimum height=22mm, align=left] (launch) at (4.5,-2.0)
  {\textbf{SniperLauncher}\\[2pt]
   $\bullet$ Sniper 생성\\
   $\bullet$ 시작/종료};

% Composition arrows from Main
\draw[arr] (m.east) -- (house.west);
\draw[arr] (house.east) -- (launch.west);

\end{tikzpicture}
\end{document}
