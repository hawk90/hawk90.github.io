\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,calc,decorations.pathreplacing}
\begin{document}
\input{../../_design.tex}
\input{../../_design-sequence.tex}
\begin{tikzpicture}[blog, seq]

% Title
\node[font=\sffamily\bfseries\large] at (4,1.4) {First Acceptance Test — Sequence};

% Actors
\node[actor] (test)    at (0,0) {Test};
\node[actor] (sniper)  at (4,0) {Sniper};
\node[actor] (auction) at (8,0) {Auction};

% Lifelines
\draw[lifeline] (test.south)    -- ++(0,-9);
\draw[lifeline] (sniper.south)  -- ++(0,-9);
\draw[lifeline] (auction.south) -- ++(0,-9);

% Messages
\draw[msgsync] (test |- 0,-1.0) -- node[msglabel]{start auction}    (auction |- 0,-1.0);
\draw[msgsync] (test |- 0,-2.2) -- node[msglabel]{start sniper}     (sniper  |- 0,-2.2);
\draw[msgsync] (sniper |- 0,-3.4) -- node[msglabel]{JOIN}           (auction |- 0,-3.4);
\draw[msgsync] (test |- 0,-4.6) -- node[msglabel]{close auction}    (auction |- 0,-4.6);
\draw[msgsync] (auction |- 0,-5.8) -- node[msglabel]{CLOSE}         (sniper  |- 0,-5.8);

% Sniper self message — state change
\draw[msgself] (sniper.south |- 0,-6.7) ..
  controls +(0.8,-0.1) and +(0.8,-0.6) ..
  node[right=4mm, font=\footnotesize, align=left] {state := LOST}
  (sniper.south |- 0,-7.3);

\draw[msgret]  (sniper |- 0,-8.4) -- node[msglabel]{verify LOST}    (test |- 0,-8.4);

\end{tikzpicture}
\end{document}
