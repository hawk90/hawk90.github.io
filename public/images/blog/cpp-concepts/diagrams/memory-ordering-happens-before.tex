\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  op/.style={rectangle, draw=nodebord, line width=0.45pt, minimum width=40mm, minimum height=8mm, fill=white, inner sep=3pt, align=left, font=\sffamily\footnotesize\ttfamily, rounded corners=2pt},
  rel/.style={op, fill=confill, draw=conbord},
  acq/.style={op, fill=absfill, draw=absbord},
  norm/.style={op, fill=impfill, draw=impbord}
]

% Column headers
\node[font=\sffamily\bfseries\small, text=inkc] at (-3, 4.6) {Thread A (producer)};
\node[font=\sffamily\bfseries\small, text=inkc] at ( 3, 4.6) {Thread B (consumer)};

% Timelines
\draw[draw=nodebord, line width=0.5pt] (-3, 4.2) -- (-3, -2.2);
\draw[draw=nodebord, line width=0.5pt] ( 3, 4.2) -- ( 3, -2.2);

% Thread A
\node[norm] (a1) at (-3, 3.5) {data = 42;};
\node[norm] (a2) at (-3, 2.3) {flag\_aux = true;};
\node[rel]  (a3) at (-3, 1.1) {ready.store(1, release)};

% Thread B
\node[acq]  (b1) at ( 3, 1.1) {while (!ready.load(acquire));};
\node[norm] (b2) at ( 3, -0.1) {read data $\rightarrow$ 42 OK};
\node[norm] (b3) at ( 3, -1.3) {read flag\_aux $\rightarrow$ true OK};

% Sequenced-before within thread (dashed)
\draw[uses, dashed, draw=mutec] (a1) -- (a2);
\draw[uses, dashed, draw=mutec] (a2) -- (a3);
\draw[uses, dashed, draw=mutec] (b1) -- (b2);
\draw[uses, dashed, draw=mutec] (b2) -- (b3);

% Synchronizes-with edge (the key one) — straight horizontal between ops, label above
\draw[focus, line width=0.9pt, -{Stealth[length=2.8mm]}] (a3.east) -- (b1.west);
\node[font=\sffamily\footnotesize\bfseries, text=focusc, fill=white, inner sep=2pt] at (0, 1.65) {synchronizes-with};

% Happens-before transitive note (textual, no arrow to avoid clutter)
\node[font=\sffamily\scriptsize, text=impbord, align=center] at (0, 0.4) {$\Rightarrow$ a1, a2 happens-before b2, b3};

% Caption box
\node[font=\sffamily\footnotesize, text=inkc, align=center, text width=160mm] at (0, -3.0) {%
  release store가 acquire load와 결합되면, store \textbf{이전}의 모든 쓰기가\\[2pt]
  load \textbf{이후}의 모든 읽기에 visible. 별도 \texttt{mutex} 없이도 안전한 publish.};

\node[font=\sffamily\footnotesize, text=warnbord, align=center, text width=160mm] at (0, -3.9) {%
  relaxed만 쓰면 이 보장 없음 $\rightarrow$ B가 \texttt{flag\_aux=true} 보다 옛 값 보일 수 있음};

\end{tikzpicture}
\end{document}
