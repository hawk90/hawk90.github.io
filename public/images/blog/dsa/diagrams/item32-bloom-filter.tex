\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  bit/.style={rectangle, draw=black!55, line width=0.4pt, minimum size=8mm, inner sep=0pt,
              fill=nodefill, font=\sffamily\small},
  set/.style={bit, fill=hifill, draw=hibord, font=\sffamily\bfseries}
]

% --- 16-bit array; bits 2, 5, 8 are set (one per hash function) ---
\foreach \i/\v in {0/0, 1/0, 2/1, 3/0, 4/0, 5/1, 6/0, 7/0, 8/1, 9/0, 10/0, 11/0, 12/0, 13/0, 14/0, 15/0} {
  \pgfmathparse{\v == 1 ? "set" : "bit"}
  \edef\ty{\pgfmathresult}
  \node[\ty] (b\i) at (\i*0.85, 0) {\v};
  \node[font=\sffamily\scriptsize] at (\i*0.85, -0.55) {\i};
}

% --- Input ---
\node[con, minimum width=24mm] (k) at (5*0.85, 2.8) {key = "apple"};

% --- Hash function arrows with labels on the curves ---
\draw[arr] (k) to[bend right=20] node[midway, sloped, above, font=\tiny]{$h_1$} (b2.north);
\draw[arr] (k) -- node[near end, right, font=\tiny]{$h_2$} (b5.north);
\draw[arr] (k) to[bend left=20]  node[midway, sloped, above, font=\tiny]{$h_3$} (b8.north);

% --- Annotation ---
\node[anchor=west, font=\sffamily\footnotesize, align=left] at (0, -1.8) {%
  \textbf{Add(x):} 모든 $h_i(x)$ 비트 set\\[8pt]
  \textbf{Contains(x):} 모든 비트 set $\Rightarrow$ "probably yes"\\[4pt]
  \phantom{\textbf{Contains(x):}} 어느 비트라도 0 $\Rightarrow$ "definitely no"
};
\end{tikzpicture}
\end{document}
