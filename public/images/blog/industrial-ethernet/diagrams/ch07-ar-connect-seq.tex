\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\input{../../_design-sequence.tex}
\begin{tikzpicture}[blog, seq]

\node[actor]    (ctl) at (0,   0) {Controller (PLC)};
\node[actorAlt] (dev) at (8,   0) {Device};

\draw[lifeline] (ctl) -- ++(0,-9);
\draw[lifeline] (dev) -- ++(0,-9);

% 1. Connect
\draw[msgsync] (ctl |- 0,-1.0) -- node[msglabel, align=center]{1. Connect (RPC) \\[2pt] IOCR Block, ExpectedSubmodule, AlarmCR Block} (dev |- 0,-1.0);

% 2. Connect Response
\draw[msgret]  (dev |- 0,-2.4) -- node[msglabel]{2. Connect Response (OK)} (ctl |- 0,-2.4);

% 3. Write multiple
\draw[msgsync] (ctl |- 0,-3.6) -- node[msglabel, align=center]{3. Write multiple (RecordData) \\[2pt] parameters per submodule} (dev |- 0,-3.6);

% 4. DControl
\draw[msgsync] (ctl |- 0,-5.0) -- node[msglabel]{4. DControl (Done)} (dev |- 0,-5.0);

% 5. CControl
\draw[msgret]  (dev |- 0,-6.0) -- node[msglabel]{5. CControl (Application Ready)} (ctl |- 0,-6.0);

% 6. cyclic
\node[seqnote, anchor=north] at (4,-7.0)
  {6. \textbf{Cyclic IO 시작} — Device LED 녹색};

\end{tikzpicture}
\end{document}
