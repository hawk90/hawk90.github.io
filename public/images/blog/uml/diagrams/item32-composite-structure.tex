\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  part/.style={con, align=center, font=\footnotesize, minimum width=32mm, minimum height=14mm},
  port/.style={rectangle, draw=edgec, fill=white, minimum size=3mm, inner sep=0pt, line width=0.5pt},
  conn/.style={draw=edgec, line width=0.7pt}
]

% Parts laid out manually
\node[part] (engine) {engine : Engine};
\node[part, right=24mm of engine] (trans) {transmission : Transmission};
\node[part, below=18mm of $(engine)!0.5!(trans)$, minimum width=80mm] (wheels) {wheels : Wheel [4]};

% Ports on engine
\node[port] (e_in) at (engine.west) {};
\node[port] (e_out) at (engine.east) {};
% Ports on trans
\node[port] (t_in) at (trans.west) {};
\node[port] (t_out) at (trans.south) {};

% External port on Car (left, delegate)
\node[port] (car_in) at ($(engine.west)+(-16mm,0)$) {};
\node[font=\tiny, left=0.5mm of car_in] {fuelIn};

% Internal port on wheels (top)
\node[port] (w_in) at (wheels.north) {};

% Connectors
\draw[conn] (car_in) -- node[above, font=\tiny] {«delegate»} (e_in);
\draw[conn] (e_out) -- node[above, font=\tiny] {torque} (t_in);
\draw[conn] (t_out) -- node[right, font=\tiny] {drive} (w_in);

% Outer Car class frame (background)
\begin{pgfonlayer}{background}
  \node[hi, fit=(engine)(trans)(wheels)(car_in),
        inner sep=10mm, fill=hifill!12, draw=hibord, line width=0.7pt] (car) {};
  \node[anchor=north west, font=\small\bfseries, draw=hibord, fill=hifill,
        inner sep=3pt]
    at ([xshift=4pt, yshift=-4pt] car.north west) {Car};
\end{pgfonlayer}

\end{tikzpicture}
\end{document}
