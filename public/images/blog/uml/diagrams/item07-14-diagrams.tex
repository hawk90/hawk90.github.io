\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,trees}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  level 1/.style={sibling distance=130mm, level distance=14mm},
  level 2/.style={sibling distance=16mm, level distance=14mm},
  leaf/.style={font=\scriptsize, minimum width=14mm}
]

\node[hi] (root) {UML 2.5.1 Diagrams (14)}
  child { node[abs] (s) {Structural (7)}
    child { node[con, leaf] {Class} }
    child { node[con, leaf] {Object} }
    child { node[con, leaf] {Package} }
    child { node[con, leaf] {Component} }
    child { node[con, leaf] {Composite} }
    child { node[con, leaf] {Deployment} }
    child { node[con, leaf] {Profile} }
  }
  child { node[abs] (b) {Behavioral (7)}
    child { node[imp, leaf] {Use Case} }
    child { node[imp, leaf] {Activity} }
    child { node[imp, leaf] {State Machine} }
    child { node[imp, leaf] {Sequence} }
    child { node[imp, leaf] {Communic.} }
    child { node[imp, leaf] {Interact.\,Overview} }
    child { node[imp, leaf] {Timing} }
  };

\end{tikzpicture}
\end{document}
