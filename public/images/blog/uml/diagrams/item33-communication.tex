\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  obj/.style={con, align=center, font=\footnotesize, minimum width=28mm, minimum height=10mm},
  link/.style={draw=edgec, line width=0.5pt},
  msg/.style={-{Stealth[length=2.4mm]}, draw=edgec, line width=0.7pt},
  msglabel/.style={font=\scriptsize, fill=white, inner sep=1.2pt, draw=none}
]

% Customer object (left)
\node[obj] (cust) {:Customer};

% Order object (center)
\node[obj, right=32mm of cust] (order) {:Order};

% Inventory (top-right)
\node[obj, above right=14mm and 14mm of order] (inv) {:Inventory};

% Payment (bottom-right)
\node[obj, below right=14mm and 14mm of order] (pay) {:Payment};

% Structural links (underneath)
\draw[link] (cust.east) -- (order.west);
\draw[link] (order.north east) -- (inv.south west);
\draw[link] (order.south east) -- (pay.north west);

% Message arrows (offset above/beside links)
\draw[msg] ([yshift=3mm]cust.east) -- ([yshift=3mm]order.west)
  node[msglabel, midway, above] {\textbf{1}: checkout()};

\draw[msg] ($(order.north east)+(2mm,1mm)$) --
           ($(inv.south west)+(2mm,1mm)$)
  node[msglabel, midway, sloped, above] {\textbf{1.1}: reserve()};

\draw[msg] ($(order.south east)+(2mm,-1mm)$) --
           ($(pay.north west)+(2mm,-1mm)$)
  node[msglabel, midway, sloped, below] {\textbf{1.2}: charge()};

\end{tikzpicture}
\end{document}
