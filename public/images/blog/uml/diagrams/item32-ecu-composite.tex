\documentclass[border=10pt,tikz]{standalone}
\usepackage{tikz}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog, x=1cm, y=1cm,
  outer/.style={cls, minimum width=130mm, minimum height=52mm,
                fill=hifill!20, draw=hibord, anchor=south west},
  part/.style={cls, minimum width=42mm, minimum height=11mm,
               font=\sffamily\footnotesize, fill=okfill, draw=okbord},
  bus/.style={cls, minimum width=22mm, minimum height=34mm,
              font=\sffamily\footnotesize, fill=absfill, draw=absbord},
  iflbl/.style={font=\sffamily\scriptsize, fill=white, inner sep=1pt},
  provball/.style={circle, draw=edgec, fill=white, line width=0.55pt, minimum size=3.2mm},
  link/.style={draw=edgec, line width=0.55pt}
]

\node[outer] (ecu) at (0, 0) {};
\node[font=\sffamily\bfseries\footnotesize, anchor=north west] at ($(ecu.north west) + (0.2, -0.15)$) {ECU};

% Parts on the left
\node[part] (cpu) at (3, 3.7) {part cpu : Cortex-M7};
\node[part] (can) at (3, 2.3) {part can : CANController};
\node[part] (adc) at (3, 0.9) {part adc : ADC};

% Bus on the right (tall, serves all parts)
\node[bus] (bus) at (10.5, 2.3) {bus : AXI};

% Provided ball on bus (left edge)
\node[provball] (pb) at (bus.west) {};

% Vertical trunk that all parts join, then one link to the bus ball
\coordinate (trunk) at (7, 2.3);
\draw[link] (cpu.east) -| (trunk);
\draw[link] (adc.east) -| (trunk);
\draw[link] (can.east) -- (trunk);
\draw[link] (trunk) -- node[iflbl, midway, above] {IBus} (pb);

\end{tikzpicture}
\end{document}
