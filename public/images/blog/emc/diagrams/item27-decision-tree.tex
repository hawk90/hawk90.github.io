\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog,
  c/.style={cls, draw=conbord, fill=confill}
]
\node[q] (q1)  at (0, 0)        {오버로드 필요?};
\node[c] (one) at (-5, -2.2)    {1. 단일 보편 참조};
\node[q] (q2)  at ( 4, -2.2)    {함수 vs 생성자};
\node[c] (sep) at (-1, -4.4)    {1' 함수 이름 분리};
\node[q] (q3)  at ( 7, -4.4)    {효율 요구};
\node[c] (cref) at ( 1,  -6.6)  {2. const T\&};
\node[c] (val)  at ( 4,  -6.6)  {3. by-value};
\node[c] (tag)  at ( 7,  -6.6)  {4. tag dispatch};
\node[c] (en)   at ( 10, -6.6)  {5. enable\_if};

\draw[uses] (q1.west)  -- node[above, font=\tiny]{N} (one.north);
\draw[uses] (q1.east)  -- node[above, font=\tiny]{Y} (q2.north);
\draw[uses] (q2.west)  -- node[above, font=\tiny]{함수} (sep.north);
\draw[uses] (q2.east)  -- node[above, font=\tiny]{생성자} (q3.north);

\draw[uses] (q3.south) -- (cref.north);
\draw[uses] (q3.south) -- (val.north);
\draw[uses] (q3.south) -- (tag.north);
\draw[uses] (q3.south) -- (en.north);
\end{tikzpicture}
\end{document}
