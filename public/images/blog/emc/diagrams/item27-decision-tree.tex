\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes}
\begin{document}
\begin{tikzpicture}[
  font=\sffamily\small,
  q/.style={diamond, draw=black!55, line width=0.4pt, fill=yellow!18,
            inner sep=2pt, aspect=2.5, align=center},
  c/.style={rectangle, rounded corners=2pt, draw=black!55, line width=0.4pt,
            inner sep=4pt, fill=red!12, align=center},
  e/.style={->, draw=black!60, line width=0.45pt, >={Stealth[length=2mm]}}
]
\node[q] (q1) at (0, 0)         {오버로드 필요?};
\node[c] (one) at (-5, -2)      {1. 단일 보편 참조};
\node[q] (q2) at ( 4, -2)       {함수 vs 생성자};
\node[c] (sep) at (-1, -4)      {1' 함수 이름 분리};
\node[q] (q3) at ( 7, -4)       {효율 요구};
\node[c] (cref) at ( 1, -6)     {2. const T\&};
\node[c] (val) at ( 4, -6)      {3. by-value};
\node[c] (tag) at ( 7, -6)      {4. tag dispatch};
\node[c] (en) at ( 10, -6)      {5. enable\_if};

\draw[e] (q1.west)  -- node[above, font=\tiny]{N} (one.north);
\draw[e] (q1.east)  -- node[above, font=\tiny]{Y} (q2.north);
\draw[e] (q2.west)  -- node[above, font=\tiny]{함수} (sep.north);
\draw[e] (q2.east)  -- node[above, font=\tiny]{생성자} (q3.north);

\draw[e] (q3.south) -- (cref.north);
\draw[e] (q3.south) -- (val.north);
\draw[e] (q3.south) -- (tag.north);
\draw[e] (q3.south) -- (en.north);
\end{tikzpicture}
\end{document}
