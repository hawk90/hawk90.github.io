\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog]

% Top row — sensing & planning
\node[abs, minimum width=36mm, minimum height=14mm, align=center] (sensors)
  at (-5, 2) {Sensors\\\small 데이터 수집·전처리};
\node[imp, minimum width=36mm, minimum height=14mm, align=center] (position)
  at ( 0, 2) {Position\\\small 위치 계산·융합};
\node[hi,  minimum width=36mm, minimum height=14mm, align=center] (route)
  at ( 5, 2) {Route\\\small 항로 계획·이탈 감지};

% Bottom row — verification & UX
\node[warn, minimum width=36mm, minimum height=14mm, align=center] (integrity)
  at (-5,-1) {Integrity Monitor\\\small 무결성 검증};
\node[con,  minimum width=36mm, minimum height=14mm, align=center] (display)
  at ( 0,-1) {Display\\\small 시각화·UI};
\node[clibord, draw=clibord, fill=clifill, rounded corners=2pt,
      line width=0.4pt, minimum width=36mm, minimum height=14mm,
      align=center, inner sep=3pt] (alert)
  at ( 5,-1) {Alert\\\small 경고 생성·우선순위};

% Inter-subsystem connections
\draw[arr] (sensors)   -- (position);
\draw[arr] (position)  -- (route);
\draw[arr] (sensors)   -- (integrity);
\draw[arr] (position)  -- (display);
\draw[arr] (route)     -- (alert);
\draw[arr] (integrity) -- (display);
\draw[arr] (display)   -- (alert);

% Outer panel
\node[panel, fit=(sensors)(position)(route)(integrity)(display)(alert),
      inner sep=10pt] (outer) {};
\node[panelTitle] at (outer.north west) {Navigation System};

\end{tikzpicture}
\end{document}
