\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds,calc,decorations.pathreplacing}
\begin{document}
\input{../../_design.tex}
\input{../../_design-state.tex}
\begin{tikzpicture}[blog, state]

\node[font=\sffamily\bfseries\small, text=inkc] at (5.5, 3.8) {SemiFuture $\to$ .via(executor) $\to$ Future};

% SemiFuture
\node[stateAbs, minimum width=30mm, minimum height=12mm] (sf) at (0.8, 1.8) {SemiFuture$<$T$>$\\[2pt]\textit{no executor}};

% Executor box (middle)
\node[stateBox, draw=hibord, fill=hifill, minimum width=30mm, minimum height=12mm] (ex) at (5.5, 1.8) {Executor\\[2pt]CPUThreadPool};

% Future
\node[stateImp, minimum width=30mm, minimum height=12mm] (fu) at (10.2, 1.8) {Future$<$T$>$\\[2pt]\textit{bound to executor}};

% Transition arrows
\draw[trans, line width=0.6pt] (sf.east) -- node[transLabel]{.via(ex)} (ex.west);
\draw[trans, line width=0.6pt] (ex.east) -- node[transLabel]{binds} (fu.west);

% Below: explanation
\node[font=\sffamily\footnotesize, text=mutec, anchor=west] at (0.0, 0.4)
  {SemiFuture 는 callback 어디서 실행될지 미지정 — .via() 호출 시점에 executor 결정};

\end{tikzpicture}
\end{document}
