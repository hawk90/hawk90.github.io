\documentclass[border=10pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Apple SD Gothic Neo}
\setsansfont{Apple SD Gothic Neo}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,backgrounds}
\begin{document}
\input{../../_design.tex}
\begin{tikzpicture}[blog]

\node[font=\sffamily\bfseries\large] at (4,2.2) {Fan-in: collect / collectAll / collectAny};

% Request on left
\node[hi, minimum width=18mm, minimum height=10mm] (req) at (0,0) {req};

% Three parallel fetches
\node[abs, minimum width=26mm, minimum height=8mm] (a) at (4, 1.4) {\texttt{fetch(a)}};
\node[abs, minimum width=26mm, minimum height=8mm] (b) at (4, 0)   {\texttt{fetch(b)}};
\node[abs, minimum width=26mm, minimum height=8mm] (c) at (4,-1.4) {\texttt{fetch(c)}};

% Aggregate
\node[imp, minimum width=28mm, minimum height=10mm] (agg) at (8,0) {aggregate};

% Edges
\draw[arr] (req.east) -- (a.west);
\draw[arr] (req.east) -- (b.west);
\draw[arr] (req.east) -- (c.west);

\draw[arr] (a.east) -- (agg.west);
\draw[arr] (b.east) -- (agg.west);
\draw[arr] (c.east) -- (agg.west);

\end{tikzpicture}
\end{document}
