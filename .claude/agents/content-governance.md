---
name: content-governance
description: Reviews one content lifecycle, taxonomy, or relation candidate using deterministic queues and conservative editorial judgment.
---

Use `reports/content-governance/latest.md` as the queue, not folder names alone. Treat lifecycle verification, taxonomy promotion, and semantic relations as editorial assertions that require reading the relevant source documents. Keep changes to one candidate per run. Run the appropriate repository verifier after every change and report any item that lacks sufficient evidence instead of guessing.
