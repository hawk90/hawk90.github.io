name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths-ignore:
      - 'README.md'
      - 'CHANGELOG.md'
      - 'LICENSE*'
      - '.gitignore'
      - 'docs/**'
      - '_jekyll_backup/**'
      - '**.txt'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"

      - name: Cache node_modules
        id: node-cache
        uses: actions/cache@v5
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}

      - name: Cache Astro content layer
        uses: actions/cache@v5
        with:
          path: .astro
          key: ${{ runner.os }}-astro-${{ hashFiles('astro.config.mjs', 'src/content.config.ts') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-astro-${{ hashFiles('astro.config.mjs', 'src/content.config.ts') }}-
            ${{ runner.os }}-astro-

      - name: Cache OG images
        uses: actions/cache@v5
        with:
          path: |
            public/og
            public/og-default.png
            .cache/og-manifest.json
          key: ${{ runner.os }}-og-${{ hashFiles('src/lib/og-themes.data.mjs', 'scripts/build-og.mjs') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-og-${{ hashFiles('src/lib/og-themes.data.mjs', 'scripts/build-og.mjs') }}-
            ${{ runner.os }}-og-

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Install dependencies
        if: steps.node-cache.outputs.cache-hit != 'true'
        run: npm ci --prefer-offline --no-audit

      - name: Build with Astro
        run: npm run build
        env:
          NODE_OPTIONS: --max-old-space-size=6144
          ASTRO_TELEMETRY_DISABLED: "1"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 5
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
